# Project State — Den Seje App (Avatar / C2)

_Authoritative project-state snapshot. Update at the end of each major section._
_Last updated: 2026-06-15_
_Status sections corrected 2026-06-30 (Section 157AB): `AVATAR_V2` is now **active** in
production — the 2026-06-15 "C2 not active / flag OFF" statements below are annotated as
**superseded**, not deleted. The **decision register (D-001…D-041), timestamps, risks, debt and
open questions remain historically unchanged.** Canonical current avatar/activation state lives in
[`AVATAR_SYSTEM.md`](./AVATAR_SYSTEM.md); cross-track status in [`ROADMAP.md`](./ROADMAP.md)._
_Platform / Services track completion added 2026-07-01 (Section 157S) — see the section immediately below._

---

## Platform / Services Track (157x) — COMPLETE (zero-cost, default-off)

_Added 2026-07-01. This is the newer **Platform / Services track** (parallel to the Avatar/C2 track
below). Full detail in [`ROADMAP.md`](./ROADMAP.md); ops/privacy specifics in
[`OBSERVABILITY.md`](./OBSERVABILITY.md) and [`157q-consent-gdpr.md`](./157q-consent-gdpr.md)._

**Status: every buildable zero-cost service is DONE — all default-off, fail-soft, PII-safe,
unit-tested. Nothing is activated; production behaviour is unchanged.**

Completed sections (from the 157A audit):
- **Docs foundation:** 157A (audit) · 157AA (PROJECT_VISION/ARCHITECTURE/ROADMAP/AI_GUIDELINES/AVATAR_SYSTEM/CLAUDE_WORKFLOW) · 157AB (consolidation).
- **Observability:** 157B (Sentry frontend) · 157C (Edge `_shared/monitoring.ts`) · 157CA (activation plan + static validation) — **default-off**.
- **Cloudinary:** 157F (decision: fetch-mode, no secret) · 157G (`js/cloudinary.js`, raster-only, inert until 167A) — **default-off**.
- **OCR:** 157H (spec) · 157I (`js/ocr/` browser-only document-recognition service, no image upload) — **default-off**.
- **AI:** 157K (`_shared/ai/` abstraction + advisory `grade-answer`; not wired to the reward path) — **default-off**.
- **Analytics:** 157D (`js/analytics.js` + GDPR consent gate) · 157E (core events + consent banner) — **default-off**.
- **Read-aloud (TTS):** 157N (decision) · 157O (`js/read-aloud/` Piper-clip + on-device Web Speech; clips = offline deliverable) — **default-off**.
- **Cross-cutting:** 157Q (consolidated consent SoT `js/consent.js` + canonical privacy map) · 157R (flag hardening `js/flags.js` + rollback runbook) · 157S (21 default-off/fail-soft unit tests via built-in `node --test` + `deno test`, no new framework).

New feature flags (all **default-off** except where noted; see [`157r-feature-flags.md`](./157r-feature-flags.md)):
- Frontend: `ENABLE_SENTRY`, `ENABLE_OCR`, `ENABLE_CLOUDINARY`, `ENABLE_ANALYTICS`, `ENABLE_READ_ALOUD` — plus the consent SoT (`js/consent.js`).
- Edge (env): `ENABLE_SENTRY_EDGE`, `ENABLE_AI_GRADING`.
- Diagnostics: `window.__flags()` reports live state (all services off; `AVATAR_V2` on).

**Gates on remaining services work (not buildable now):**
- **157CB — dedicated staging environment** (Supabase branch + Vercel preview) is the prerequisite
  for **activating/validating** any external service (Sentry/PostHog/AI). Policy: no external service
  is activated or validated against production. Free interim = local Supabase stack + Vercel preview.
- **Deferred/future infra:** 157L/157M (AI grade wired into `process-event` + teacher UI), 157P
  (Whisper STT), 157T (production-readiness). Piper audio clips + the 167A avatar art are **offline
  deliverables**.

---

## Current Production State
- **Supabase:** project `den-seje-app` (ref `tjzbehwfagiwpwodsgwg`), region eu-west-1, plan **Pro**.
- **Data:** 31 profiles; 5 with equipped cosmetics; 2 with an explicit legacy hairstyle.
- **Frontend:** Vercel (`den-seje-app-frontend.vercel.app`), auto-deploys from GitHub `main`.
- **Avatar shown to users (as of 2026-06-15):** **LEGACY** render. C2 built but not active.
  > **STATUS UPDATE — 2026-06-30 (Section 157AB): SUPERSEDED.** `AVATAR_V2 = true` is now **live in
  > production** (commit `52f8365`, 2026-06-25). The **C2 render path is the default** for users.
  > However it still renders **flat placeholder SVGs**, **not** the Northstar Master raster — Master
  > asset production + wiring remain **future work** (`docs/167a-master-asset-raster-wiring-plan.md`).
  > Canonical: [`AVATAR_SYSTEM.md`](./AVATAR_SYSTEM.md) §2.

## Current Commit
- `origin/main` = **`4029594`** ("docs: eye system ADR + hybrid raster pipeline ADR (D-012..D-019)").
- Frontend clone and ROOT clone both at `4029594` (in sync).
- One GitHub repo: `Moeller888/den-seje-app-frontend`. The ROOT clone embeds the
  frontend as a vestigial gitlink that is **not** on the Vercel deploy path.

## Feature Flags
- **`AVATAR_V2` (as of 2026-06-15): `false` (OFF)** — `js/avatar-layers.js:230`.
  > **STATUS UPDATE — 2026-06-30 (Section 157AB): SUPERSEDED.** `AVATAR_V2` is now **`true` (ON)**
  > (`js/avatar-layers.js:230`; live since commit `52f8365`, 2026-06-25). `isAvatarV2()` returns
  > `true` from the constant, else honours the `localStorage.avatar_v2='1'` override.
- Per-browser test override: `localStorage.avatar_v2='1'`.
- **No cohort / percentage rollout mechanism exists.** (Still true — open question OQ-4.)

## Current Avatar System
- **Legacy (LIVE):** `body.svg` + hair + equipped cosmetics; pseudo-3D/gradient style.
- **C2 (BUILT, flag-gated, INACTIVE):** flat blank-canvas base (medium/dark) +
  identity hair (7) + hair-color tokens (8, inline) + cosmetics parity + z-model.
  Single shared render module `js/avatar-render-c2.js` (`mountC2Avatar`), wired into
  avatar.html, hub.html, app.js, shop.html — all gated by `isAvatarV2()`.

## C2 Status
> **STATUS UPDATE — 2026-06-30 (Section 157AB): C2 IS NOW ACTIVATED.** `AVATAR_V2 = true` shipped to
> production on 2026-06-25 (commit `52f8365`). The bullet below described the **pre-activation**
> (2026-06-15) state and is retained for history. Current state: C2 render is **live and default**,
> but on **flat placeholder SVGs** — the Northstar Master raster is unbuilt/unwired (future work,
> `docs/167a-master-asset-raster-wiring-plan.md`). Canonical: [`AVATAR_SYSTEM.md`](./AVATAR_SYSTEM.md) §2.
- **C2 is technically implemented but NOT activated** (`AVATAR_V2 = OFF`). _(historical — 2026-06-15)_
- Sections 155B–160 complete. DB migrations 155E (hair_color) + 155F (hairstyle
  alignment) applied to production. Render pipeline + cosmetics parity committed
  and pushed. Goldens in `tests/c2-golden/`.
- Activation status: **YELLOW** — ready for internal test; not for full activation.
- **Known gap:** the implemented flat-SVG C2 base does NOT match the chosen design
  reference on its defining traits (eye size, art finish) — see `avatar-vision.md`.

## Art Direction Decision (NEW)
- **SVG-only is REJECTED as the final art strategy.** Flat SVG cannot deliver the
  desired avatar art quality (large expressive eyes + premium anime-inspired finish).
- **Direction:** **Hybrid Raster** — produce the North Star character as WebP layers
  in the existing format-agnostic pipeline; reuse DB / identity / slot / z-model /
  AVATAR_V2 (Section 163A). North Star Avatar v1.0 is the permanent visual target.
- **Pipeline LOCKED (ADR-163D / D-013…D-019):** WebP assets · hair color = hybrid
  luminance-map tint · eye color = iris-base tint (fixed highlight) · skin tone =
  separate base assets · hybrid loading · immutable versioned cache · mobile perf
  budget. Architecture (DB/identity/slots/z-model/AVATAR_V2) unchanged.

## Avatar Layer Model (Hybrid Raster — current)
Ordered render layers (reuse the existing z-model):
1. **Base body** (skin + neutral underlayer + head, NO face) — per skin tone — z 0–2
2. **Face/Expression** (brows, nose, mouth, multiply blush — NO eyes, NO skin) — z 3
3. **Eyes** (separate, tintable iris + fixed highlight — see D-012) — **z 4 (D-030)**
4. **Blink** (eyelid, engine) — z 5
5. **Hair** (luminance map + tint; `hair-northstar-v1`, D-031) — z 40
6. **Cosmetics** (equipped_slots at C2_LAYER_Z)

> This model **supersedes Section 163A's "eyes embedded in Face/Expression"** —
> eyes are now their own layer (D-012 / ADR-163B). Render stack locked by D-030;
> full decomposition spec in `docs/adr/ADR-163F-raster-asset-spec.md`.
>
> **MVP base (D-040):** the layer model above is the **full raster target**, but for MVP
> the **Tier-0 base is `Northstar Master.png` as-is** (fixed default avatar) + accessory
> overlays only. The decomposed neutral base + per-skin-tone/face/eyes/hair layers are a
> **deferred upgrade**, not an MVP blocker.

## Major Decisions (register)
| ID | Decision |
|---|---|
| D-001 | Avatar direction = **C2 Base Avatar Premium** (human chibi/anime kid), chosen in a bake-off over creature/axolotl, chibi study-hero, geometric mascot, legacy redesign. |
| D-002 | Hair = identity; Hair Color = identity; Companion = not MVP; Badge = not MVP. |
| D-003 | Hair Color = inline SVG + CSS tokens (`--hair-base` / `--hair-shadow`). |
| D-004 | Asset spec: viewBox `0 0 160 240`; LOCKED anchors head `cx80 cy50 r30`, eyes `cx68/92 cy47`; flat (R1–R5, no gradients). |
| D-005 | C2 base reuses the legacy anchors (preserve blink/expression) → C2 shipped as a same-proportion restyle. |
| D-006 | Free staging not recommended (migration drift); use Supabase Branching (Pro) or direct prod apply. |
| D-007 | Direct prod apply of 155E/155F (assessed SAFE, executed, verified). |
| D-008 | Z-model reform (159B): deterministic `C2_LAYER_Z`; expr z=3, blink z=5, hair z=40. |
| D-009 | Cosmetics parity-first (render legacy assets in C2); flat redesign progressive. |
| D-010 | 159D: static neutral expression in C2 shop preview (face parity). |
| **D-011** | **SVG-only rejected as final art direction; move to hybrid/raster pipeline.** |
| D-011b | Hybrid Raster architecture: North Star as WebP layers in the existing pipeline; reuse DB/identity/slots/z-model/AVATAR_V2 (163A). North Star v1.0 = permanent visual target. |
| **D-012** | **Eye system = a SEPARATE, tintable, cosmetic-capable layer** (ADR-163B, Option C). Iris is tint-controlled (eye color = token, free); the eye layer supports future eye cosmetics, eye rarity, magic/rare eyes, glasses, masks, blink and the emotion system. **Supersedes 163A's "eyes embedded in Face/Expression".** Rationale: multiple eye colors + eye cosmetics + rarity would otherwise force a combinatorial asset explosion (expression × color × variant) and a future rewrite; a separate tintable layer is the robust, future-proof choice. |
| D-013 | Asset format = **WebP** (PNG fallback only if needed). Canonical 2:3 raster master 1024×1536 → served WebP 512×768; anchors mapped proportionally from the 160×240 geometry. (ADR-163D) |
| D-014 | Hair color = **hybrid**: canvas multiply-tint of a neutral luminance map (8 colors free as tokens) + hand-painted variant override for problem colors. (ADR-163D) |
| D-015 | Eye color = **tint the iris-base only**; sclera/pupil/glossy highlight stay fixed; rare/magic eyes = iris-swap or eye-effect overlay in the eye slot. (ADR-163D) |
| D-016 | Skin tone = **separate base assets** per `body_type × skin_tone` (cel-shaded skin is not reliably runtime-tintable; body owns skin). (ADR-163D) |
| D-017 | Asset loading = **hybrid**: eager preload of the user's own avatar; lazy-load shop catalog + other avatars. (ADR-163D) |
| D-018 | Cache = **immutable, versioned assets + manifest**; invalidation via filename version (never mutate a shipped asset). (ADR-163D) |
| D-019 | **Mobile-first performance budget**: first-paint < 100ms, full composite < 250ms, total avatar < ~350KB, decoded memory < ~15MB. (ADR-163D) |
| D-020 | **MVP = one Neutral North Star character**; body-type system preserved architecturally (additive later, no rewrite). (ADR-163F) |
| D-021 | **Eye granularity = 2 files**: `iris` (tintable) + `fixed` (sclera/lash/shape + fixed highlight). MVP scope; cosmetic/rarity capability (D-012) preserved. (ADR-163F) |
| D-022 | **Face/Expression layer = tone-agnostic**: no opaque skin shading (base owns skin), blush = `multiply`, carries brows/nose/mouth; one face set shared across skin tones. (ADR-163F) |
| D-023 | **Blink = WebP eyelid assets per skin tone** (eyelid shows skin → skin-bearing layer). (ADR-163F) |
| D-024 | **Expression MVP = positive only**: neutral, happy, curious, focused, determined, surprised, proud (7). `sad`/`angry` excluded — permanent **never-negative policy** (resolves OQ-6). (ADR-163F) |
| D-025 | **Hair compression ignored in MVP**; contract hook reserved: `hair_state = full \| compressed` (MVP produces `full`). (ADR-163F) |
| D-026 | **Cosmetic recolor = hybrid**: MVP = baked assets; tint = future opt-in (no cosmetic tint pipeline in MVP). (ADR-163F) |
| D-027 | **Asset canvas = full canvas, all layers**, transparent padding; no cropped/trimmed per-layer assets (one shared coordinate space). (ADR-163F) |
| D-028 | **North Star Master v1.0 = `medium` skin-tone token** (internal token, not a subjective colour reading; no new tone tokens / no rename now). (ADR-163F, 164A) |
| D-029 | **Neutral body-underlayer = a derived production asset** cut from the master; the master's sweater/jeans/sneakers are reference art (future cosmetic set), never baked into `base`. (ADR-163F, 164A) |
| D-030 | **Eye layer z-index = 4**; render stack locked: base z0–2 · face z3 · **eyes z4** · blink z5 · hair z40 · cosmetics `C2_LAYER_Z`. Fills the eyes gap left by D-008. (ADR-163F, 164A) |
| D-031 | **North Star hairstyle token = `hair-northstar-v1`** (one approved hairstyle; produced as a neutral luminance map, brown = default tint). (ADR-163F, 164A) |
| **D-032** | **North Star source-of-truth split (164B-prep).** `assets/avatar/reference/Northstar Master.png` is the **SOLE authoritative geometric source** for 164B decomposition — proportions, head/body ratio, pose, hair silhouette, face, eyes, rendering style and character identity all derive from it. `Northstar Master - reference.png` is **downgraded to an outfit-style reference ONLY** (neutral clothing direction: plain t-shirt / plain trousers / plain sneakers). It **MUST NOT** be used as a source of truth for proportions, body height, hair shape, eye size, facial structure or pose. **On any conflict between the two images, `Northstar Master.png` always wins.** Rationale: four regeneration attempts of the companion all drifted taller/leaner with an altered hair silhouette — proportions cannot be locked via regeneration, so geometry is taken directly from the frozen Master. (164B-prep, 2026-06-15) |
| **D-033** | **Base production method = manual paint-over, NOT AI (164C — Base Production Method Pivot).** AI generation/inpainting **cannot reliably** produce `body-neutral-medium-v1`: it repeatedly introduces **proportion + identity drift** (confirmed across four companion regenerations **and** an explicit edit/inpaint-mode attempt — all drifted taller/leaner with altered hair and face, D-032). **Decision:** AI-generated / AI-inpainted images **MUST NOT** be used as production base assets; they may be used **only as visual outfit references**. `body-neutral-medium-v1` must be produced from a **manually controlled layered source / manual paint-over over `Northstar Master.png`** (Master = geometry, D-032), validated by the 164B.3 base-coherence gate. Sharpens R-6; refines the *method* of D-029 (the base is reconstructed by hand, never AI-derived). **Scope (see D-034):** D-033 applies to **geometry-defining rig layers only** — it does NOT ban AI for shop/cosmetic overlays. (164C, 2026-06-15) **[AMENDED by D-042 (2026-07-02): the human-paint-over-only requirement is impractical (no painter available). AI-assisted MASKED decomposition on the frozen Master is now allowed for Phase-2 rig layers; full AI regeneration/redesign remains forbidden; identity-lock preserved via masking + the 164B.3 gate.]** |
| **D-034** | **Scalable item production = slot-constrained transparent overlays; AI allowed for item overlays only, never for avatar geometry (164D).** Scopes D-033 to **geometry-defining rig layers** (base, face, eyes, blink, hair, anchor template, per-slot masks — manual, AI-forbidden as producer). **Shop/cosmetic items are full-canvas transparent overlays** bound to a slot + slot-mask + z; they contain **no** avatar geometry/skin. **AI is permitted for item overlays only** and must NEVER define body/face/hair/eyes/proportions/anchors/masks; every AI item must pass the **slot-mask + automated QA gates** before entering the catalog. Reuses the existing slot model, `equipped_slots`, `shop_items`, `RARITY_COLORS` and the immutable versioned manifest (D-018) — **additive, no rewrite**. New slots (`shoes`/`bottom`/`hands`/`front_fx`) reconciled in **D-035**. Full spec: `docs/164d-shop-pipeline.md`. (164D, 2026-06-16) **[EXTENDED by D-042 (2026-07-02) for Phase-2: masked AI-assisted decomposition of rig layers on the Master is allowed; the "AI never defines geometry" rule now means never via *unmasked regeneration* — masked edits on the frozen Master that preserve identity are permitted.]** |
| **D-035** | **Canonical slot/z model for scalable shop overlays = `C2_LAYER_Z` (164E).** The C2 z-model (`C2_LAYER_Z` + `C2_BASE_Z=0` + `C2_HAIR_Z=40`, 159B/D-008) is the **canonical** slot/z model for all scalable shop/cosmetic overlays (D-034). **Legacy `SLOT_Z`/`SLOTS` is frozen/deprecated** — retained only for the live legacy render path until the AVATAR_V2 cutover, and **MUST NOT be extended** (new slots are C2-only). **Slot *names* are the shared contract** across both z-maps and the DB (`shop_items.slot_type`, `equipped_slots` keys, equip/unequip RPCs). Cosmetic **`face` = masks** (NOT the raster face/expression layer z3); cosmetic **`eyes` = glasses** (NOT the raster eyes layer z4); **blink z5 = engine/surface** (not a shop slot); **`hair` = identity/geometry** (not a purchasable slot). Proposed slots **`shoes`/`bottom`/`hands`/`front_fx` receive RESERVED C2 z-values only (8 / 15 / 25 / 100)** — **activation DEFERRED** pending: (1) product-taxonomy decision, (2) `shop_items.layer_order` semantics, (3) DB/RPC slot-validation check, (4) per-slot mask authoring, (5) AVATAR_V2 cutover plan. Locks the **model + reservations only — no code/DB/asset change** (js/avatar-layers.js untouched). Spec: `docs/164d-shop-pipeline.md`. (164E, 2026-06-16) |
| **D-036** | **Shop product taxonomy — MVP + post-MVP (164F).** **MVP purchasable categories = `aura`, `back`, `headwear`, `face` (masks only), `eyes` (glasses only)** — accessory-first (generous masks, no base-outfit occlusion). **`torso` (tops) = CONDITIONAL MVP** — activate only once the base body + `torso` mask are produced and pass QA. **`neck` = optional / low priority.** **`hair` = identity/rig only** (hairstyle + `hair_color` tokens; **not** a purchasable overlay). **DEFERRED post-MVP:** `body` (full-body costumes — high occlusion/registration risk), `shoes`, `bottom`, `hands`, `front_fx` (front-of-face/eye occlusion risk). When activated, **`bottom` + `shoes` stay SEPARATE** (mix-and-match); `hands` waits (low ROI); `front_fx` waits. **Semantics:** `face` = cosmetic masks (≠ raster face/expression layer z3); `eyes` = cosmetic glasses (≠ eye-color/rare-eyes in the eye rig layer, D-012/D-015). Resolves D-035 deferred-activation requirement (1). **Product-architecture decision only — adds no slots, no code, no DB/RPC, no assets**; curates which *existing* slots are MVP shop categories. Spec: `docs/164d-shop-pipeline.md`. (164F, 2026-06-16) |
| **D-037** | **MVP shop item QA gates & mask spec (164G).** Every shop item is a **full-canvas transparent overlay** (master **1024×1536** → served **512×768**; no crop/trim, no per-item offset math, pure z-overlay). **Automated-first gate (HARD):** transparent bg · clean alpha/no-halo · **slot-mask compliance (0 opaque px outside mask)** · **no avatar geometry/skin/face/eyes/hair** · anchor/registration · canonical **`C2_LAYER_Z`** slot · manifest completeness · per-item performance budget (≤~50 KB, within D-019 stack) · composite smoke test (eye legibility preserved). **Human review:** style conformance + **content safety** (kids platform). **MVP slot mask rules:** `aura` = generous behind-avatar; `back` = generous shoulder-anchored; `headwear` = moderate head-anchored (**eyes legible**); `face/masks` = tight face-anchored (**eyes legible**); `eyes/glasses` = tight eye-anchored (**approved eye-overlap exception**). **`torso` mask conditional** (needs base body + occlusion mask, QA-passed); **`neck` optional/low priority**. **Locks the gate framework + mask RULES only** — **exact pixel mask assets are Tier-1 work pending the produced base/rig (164B)**; **no code/tooling/mask/assets** produced. Spec: `docs/164d-shop-pipeline.md`. (164G, 2026-06-16) |
| **D-038** | **Tier-1 base rig & mask authoring plan (164H).** Locks the Tier-1 sequence required before any scalable MVP shop-item generation: **(1) base rig `body-neutral-medium-v1`** (manual paint-over from Master, D-032/D-033; PASS 164B.3 — **blocking first step**) → **(2) anchor template** (JSON + overlay: head 512,320 r192, **revised eye box**, shoulder/back, face oval, crown region) → **(3) style kit** (palette/line/cel/light/detail/forbidden-drift; ≥2–3 exemplars/slot) → **(4) MVP slot masks** (aura/back/headwear/face/eyes; **1024×1536 QA/build artifacts, NOT runtime assets**; derived from anchors per D-037) → **(5) reference composites** (≥2/slot; style + composite goldens + eye-legibility calibration set) → **(6) QA threshold config** (eye-legibility, feather, per-item weight, decoded-memory/concurrent-layer cap, mask-overflow). **`torso` mask conditional; `neck` optional.** **No AI may define the base geometry; exact mask pixels wait for the produced base; no shop-item batch starts before all six exist.** Locks **plan/formats/naming/gates/order only — no base, masks, assets, code, or tooling produced.** Spec: `docs/164h-tier1-base-rig-mask-authoring-plan.md`. (164H, 2026-06-16) **[RE-SCOPED by D-040: base rig is no longer the MVP blocking first step — Tier-0 = Master; the next blocking step is automated/semi-automated anchor + mask extraction from Master. This Tier-1 sequence governs the future neutral-base upgrade and the Master-derived masks.]** |
| **D-039** | **Base rig production execution method (164I).** **Primary = Option B — outsource `body-neutral-medium-v1` to a professional illustrator** against a locked brief (the base is the single highest-risk foundational asset and must pass 164B.3). **Fallback = Option A** (in-house manual paint-over, budget only); **Option C** (semi-automated vector/paint-over) is a **geometry scaffold only, never the final finish**. Base rig is **manually controlled, geometry-locked to `Northstar Master.png`**; **AI must not define production geometry** (D-032/D-033). **No anchor template or masks may start until the base passes 164B.3** (D-038 order). **Handoff:** layered source + flattened review PNG + served WebP candidate + change notes + optional onion-skin/overlay vs Master. **Acceptance gate: 164B.3 PASS before becoming the datum.** **NO-GO:** taller/slimmer body, altered head size, altered face/eyes/hair, pose drift, style drift, AI-looking regeneration, cropped canvas, baked background, any unreviewed geometry change. **Planning decision only — no base/image/mask/asset/code/tooling produced.** Spec: `docs/164i-base-rig-production-execution-plan.md`. (164I, 2026-06-16) **[RE-SCOPED by D-040: this illustrator method applies ONLY to the future optional neutral-base upgrade — it is no longer the MVP path or a blocker.]** **[SUPERSEDED for producer by D-042 (2026-07-02): no illustrator available; Phase-2 rig layers are produced by AI-assisted masked decomposition on the Master, not outsourced illustration.]** |
| **D-040** | **Automation-first avatar production (164J).** The avatar shop must be **automatable end-to-end — no illustrator dependency for MVP, no manual per-item avatar editing**. **`Northstar Master.png` is the Tier-0 default base avatar/datum** (already exists, approved geometry, D-032) → **`body-neutral-medium-v1` is NOT an MVP blocker**. **MVP proceeds with accessory overlays only:** `aura`, `back`, `headwear`, `face/masks`, `eyes/glasses` (D-036). The neutral reconstructed base becomes a **future optional quality upgrade**; **D-039's illustrator method is re-scoped to that upgrade only**, and **D-038 is re-scoped** (base rig no longer the blocking step — **automated/semi-automated anchor + mask extraction from Master is now the next blocking step**). **D-033 / 164B.3 still apply if/when the neutral-base upgrade is produced.** **Safety unchanged (D-034 reinforced):** AI must **never** regenerate the full avatar or define body/face/hair/eyes/proportions/anchors/masks; AI may generate **only isolated slot-constrained transparent overlays** for approved slots, each passing the slot-mask + QA gates (D-037). **Accepted tradeoff:** MVP = one fixed avatar (the Master) + accessories; **per-user skin tone / hairstyle / hair-color variation defers** with the neutral-base upgrade. Supersedes the *blocking* status of D-038 and the *primary* status of D-039; preserves D-032/D-034/D-035/D-036/D-037. Production model recorded in `docs/164d-shop-pipeline.md` (Tier-0/Tier-1/Tier-2). (164J, 2026-06-16) |
| **D-041** | **Automated anchor template + MVP mask extraction from Master (164K).** Anchors + the 5 MVP accessory-slot QA/build masks are derived **directly from `Northstar Master.png` (verified 1024×1536)** by a **deterministic, non-AI image-processing step**: silhouette via white-matte threshold; anchors (head/eye-band/shoulder/face-oval/crown) from the ×6.4 mapping **with mandatory human confirmation of the eye band + face oval**; masks per D-037 (aura generous-behind · back generous shoulder-anchored · headwear moderate head-anchored, eyes clear · face tight face-anchored · eyes tight eye-anchored). **Protected zones** (face/eyes/hair/body/hands/skin-like) feed the no-geometry/no-skin gate. **Outputs = QA/build artifacts only** (`avatar-anchor-template-v1.json` + anchor-overlay + mask PNGs), **never runtime assets, never used to alter geometry**; **Master unchanged; no AI defines geometry/anchors/masks** (D-034); **no shop items generated**. **Acceptance before any item batch:** dimensions validated, anchors + mask previews human-approved, mask-overflow checks defined, D-037 compatibility confirmed, no runtime/AVATAR_V2 change. Locks **method/schema/artifacts/gates only — no tooling/masks/assets produced**. Next = **164L** (the non-AI extraction tooling). Spec: `docs/164k-anchor-mask-extraction-plan.md`. (164K, 2026-06-16) |
| **D-042** | **Phase-2 art policy AMENDED — AI-assisted MASKED decomposition allowed (2026-07-02).** The project owner has **no human painter/illustrator**, so the *human-paint-over-only* requirement of **D-033** (and the illustrator method of **D-039**) is **impractical** and is **amended for Phase-2 art production**. **Allowed:** AI-assisted **masked inpainting/outpainting ON the approved `Northstar Master.png` only** — lifting baked features to build the decomposed layers (v2 base, face, eyes, eyelid, hair) and reconstructing the skin/clothing they hid, **provided the output preserves the signed-off Master identity**. **Still FORBIDDEN:** full AI regeneration, redesign, "make a new avatar," and **any unmasked/broad prompt** that lets the model reinterpret the avatar; **no change** to head size, eye shape, hair silhouette, pose, outfit style, skin tone, line art, lighting, palette or proportions (D-032); any result that no longer matches the signed-off Master. **Identity-lock is preserved via masking + the 164B.3 base-coherence gate (unchanged)** — the D-033/R-6 concern (AI *regeneration* drifts identity) is mitigated by constraining AI to *masked edits on the frozen Master*, not free generation. **Gates unchanged:** gate 2 (v2 base) open until the AI-assisted base passes 164B.3; gate 3 (face/eyes/eyelid/hair) open until those pass review; gate 5 (composed visual sign-off) open; gates 1 + 4 satisfied. **No runtime/manifest/`AVATAR_R2` change; Phase-2 implementation NOT started.** Amends D-033 (method) + D-039 (producer); preserves D-032/D-034/D-040. (2026-07-02) |
| **D-043** | **Phase-2 base RECOVERY — iter7 invalidated; `recovery-base-v1-blankface.png` adopted as a registered base-layer source (2026-07-05; REVISED 2026-07-06).** The **iter7** base candidate is **visually INVALIDATED** by a corrective audit: a **skin-colored bust/chest-plate artifact** (a wide flat skin oval on the shirt, no real chin→neck→collar) — a **structural anatomy failure**, not the "minor finish" issue the earlier review recorded (the §2 "proportions PASS" relied on per-row width metrics that missed the shape; pale-on-white composites camouflaged it). The whole deterministic-carve line (iter4→iter7) is invalidated for the same reason, and the **owner-countersigned iter7 164B.3 CONDITIONAL PASS is WITHDRAWN/SUPERSEDED**. **Decision:** adopt **`source-master(3).png`** (preserved in-repo as `assets/avatar/reference/recovery-base-v1-blankface.png`, 1024×1536 RGBA) as a **candidate registered base-layer source** (registered to the frozen Master; **NOT a new Master/datum**) for the **deferred** Phase-2 base path — it fixes the failure (correct bald scalp, intact ears, proper head→neck→collar, no bust-plate, cleanly blank face, clean alpha). **`Northstar Master.png` remains the canonical identity/style/coordinate datum; D-032 is PRESERVED, not superseded.** Verified 2026-07-06 (falsification-tested): recovery-base is the **same figure at the same scale**, registering to the Master by a **deterministic translation (+25 x, +285 y)** — body silhouette **IoU ≈ 0.9921**; **≈ 84.3 %** of overlapping body pixels essentially identical to the Master. **[REVISED 2026-07-06: the earlier "separate re-rendering / different scale / non-registrable / supersedes D-032" wording was an analysis error and is withdrawn; recovery-base is a base-layer source, not a master replacement.]** **The base-layer source is NOT a runtime asset, NOT promoted, and NOT yet passed:** its outfit is **not neutral** (original green sweater+star/cargo/green shoes → must be neutralized, D-029/D-022), it must be **registered** by the known (+25,+285) translation (no re-datum, no scale change; anchors/eye-box/masks carry over by that constant offset), its **cropped lower legs/feet** completed from the Master if a full figure is needed, and it needs a **fresh 164B.3 review + composited sign-off** before Gate 2 can be satisfied. **Gate 2 REOPENED / under recovery; Gate 3 PAUSED; hair/eyes/face tooling still useful but their outputs are NOT approved layers against the new basis. `AVATAR_R2` stays `false`; no runtime/manifest/promotion change.** Full note: [`167a-phase2-base-recovery-decision.md`](./167a-phase2-base-recovery-decision.md). (2026-07-05) |
| **D-044** | **Coordinated top+underarm mask refinement for the D-042 neutral outfit (2026-07-11).** During the D-042 CPU neutral-outfit passes (shoes toolchain-proof PASS; bald assembled base built; **trousers v3 owner-accepted** — cargo pocket + orange zipper removed via deterministic pre-fill + low-denoise), the **top** region is BLOCKED: the refined **`top-edit-mask` (96,171 px)** under-covers the tee — it spans only the **central front torso panel**, leaving the collar, shoulders, both upper-arm sleeves, the orange sleeve stripes and green wristbands **green** — so a plain **light-grey short-sleeve** tee is not achievable with it. **Decision required:** the owner sets a **short-sleeve hem line** on the upper arm; `top-edit-mask` is then extended **deterministically (non-AI)** to shoulders + upper-arms **above** the hem ∩ garment, **minus the protect zone** (neck/hands; refuse-on-protect guard, proven in `extend-trousers-mask.mjs`), while the forearm **below** the hem is reassigned to the **`underarm-reconstruct-mask`** for bare-skin reconstruction (ΔE-matched to the visible hands) as its **own sub-gate**. Top + underarm masks are defined **together** (tile the arm, no gap/overlap); the neck/collar join stays with `neck-seam-feather-mask`. **Gated flow:** owner picks hem → deterministic (re)gen of both masks → owner reviews **both mask overlays before any AI pass** → top pre-fill (recolor→grey + flatten star/stripes) + low-denoise inpaint → **underarm reviewed separately** before the tee is called done. **Identity-lock preserved** (D-032 Master frozen; D-042 masked-edits-only, no unmasked regeneration; protect zone frozen). **No approved mask or `Northstar Master.png` is altered until sign-off; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.** Preserves D-032/D-042. (Rationale/evidence — `prefill-top` gap render, `mask-refinement-report.json` — captured in the session's gitignored D-042 review area.) (2026-07-11) |
| **D-045** | **Underarm bare-arm reconstruction toolchain (2026-07-12).** The D-044 underarm sub-gate — the lower arm **below the mid-upper-arm hem (y≈695)** must become **bare skin** — does **not pass** on the local **CPU / SD1.5-inpaint** pipeline. Root cause: SD1.5 masked inpaint **context-bleeds** — it pulls adjacent colours into the masked region (green→grey on the tee sleeves; **grey→skin bleed producing grey box artifacts on the arm**), a pattern documented across the trousers pocket, the top sleeves and the underarm. The **deterministic** skin-recolor produces the correct **hand-ΔE-matched skin tone** (RGB ≈ 254,189,125) but retains **sleeve-fold/cuff artifacts** (reads as a recoloured sleeve, not a smooth bare arm), and the **green wristbands are protected residual** (in the protect zone, frozen — like the trousers-pocket residual). **Decision required:** choose the underarm reconstruction toolchain — **(a)** a dedicated object-removal/inpaint model (e.g. **LaMa**) for clean fabric→skin, **(b)** **GPU + SDXL/Flux** inpainting with stronger context control, or **(c)** **manual paint-over** (consistent with D-033's AI-drift concern; no painter currently available per D-042). **Delivered so far (review-only, gitignored):** shoes toolchain-proof, bald assembled base, **trousers v3 owner-accepted**, **top tee complete** (plain grey, star/pocket removed, short sleeves at the D-044 hem), D-044 partitioned top+underarm masks, and a deterministic skin-recolor **placeholder** for the arm. **Identity-lock preserved throughout** (protect-diff = 0 on every region; D-032 Master frozen; D-042 masked-edits-only; protect zone frozen). **No approved mask or `Northstar Master.png` altered; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.** Preserves D-032/D-042/D-044. (Rationale/evidence — underarm AI grey-bleed render + deterministic skin-recolor — captured in the session's gitignored D-042 review area.) (2026-07-12) **[UPDATE 2026-07-12: option (a) LaMa executed. LaMa reconstructs a smooth bare arm (validated, protect-diff 0) and is confirmed the right tool for skin removal — but its fill over-saturates into a persistent ORANGE zone at the elbow that resists BOTH deterministic de-orange (blended tones → no clean threshold) AND a targeted LaMa thin-line cleanup pass (the zone is surrounded by the same colour, so LaMa fills orange-from-orange). The automated local CPU pipeline has reached its limit on the elbow finish. Escalated: the elbow finish needs option (b) GPU + SDXL/Flux inpainting OR (c) manual paint-over (a few-hundred-px touch-up). The rest of the neutral outfit is complete — bald base → plain grey short-sleeve tee → charcoal trousers → grey low sneakers → LaMa skin arms, protect-diff 0 throughout; the green wristbands remain a protected residual. No approved mask or `Northstar Master.png` altered; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.]** |
| **D-046** | **Green wristbands — protect-zone vs neutral-outfit conflict (2026-07-12).** The D-042 neutral-outfit **164B.3 review (owner-countersigned FAIL, 2026-07-12)** flags the **green wristbands** as a remaining **signature-outfit element** (§5 FAIL, D-029/D-022 require a plain outfit). But the wristbands sit **INSIDE the identity-lock protect zone** — the wrist region adjacent to the protected hands — so they **cannot be removed by a masked edit** without narrowing the protect mask. This is a genuine conflict between the neutral-outfit requirement and the identity-lock (freeze hands/wrists), which is why every automated pass left them untouched (like the trousers-pocket residual). **Decision required:** **(a)** deterministically **carve a tight wristband edit-mask** out of the protect zone (the wristbands are garment-green, distinguishable from hand skin) — review the carve overlay first — then recolor them to arm/skin; **(b)** **manual paint-over** of the wristbands (same package pattern as the elbow, D-045 c); or **(c)** **accept** the wristbands as out-of-scope for the neutral base (they read as thin cuffs). **Recommendation:** (a), a tight review-first carve — the smallest change that keeps the hand identity frozen. **No approved mask or `Northstar Master.png` altered until sign-off; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.** Preserves D-032/D-042/D-044/D-045. (Evidence: `d042-164b3-review.md` §5, session's gitignored D-042 review area.) (2026-07-12) **[UPDATE 2026-07-12: option (a) carve EXECUTED. A deterministic tight wristband edit-mask (garment-green in the wrist boxes, with skin AND protect hard-excluded) recoloured 2910 px → arm skin; validated protect-diff 0, outside-carve changed 0. **Premise correction:** the carve showed the wristbands were **mostly NOT in the protect zone** — only **65 of 2975** green px overlapped protect (the hand-adjacent edge, kept green/frozen so protect-diff stays 0). They survived every earlier pass because of a **MASK GAP** (below the underarm skin-Y cap, above the trousers), **not** because they were protected — the "inside the protect zone" framing above is corrected. §5 wristband item is now **cleared** on `d042-outfit-candidate-nowrist.png` (faint band mark + the 65-px protected edge residual remain). The **elbow finish (§4, D-045 b/c)** is the last punch-list item before a 164B.3 re-run. No approved mask/`Northstar Master.png` altered; `AVATAR_R2` false; Gate 2 NOT satisfied; Gate 3 PAUSED.]** |
| **D-047** | **Generated neutral-base candidate v3 admitted to a full 164B.3 review — NARROW review exception (2026-07-12).** A ChatGPT-image-generated neutral-base candidate (**"v3"**, from `source-master.png` as *appearance* reference only) is **permitted to enter a full 164B.3 base-coherence review** — **solely because it is the first broad-generation candidate to match the Northstar heads-tall proportion.** Measured **heads-tall 2.98 vs Northstar 3.03** (Δ 0.05, inside the ±0.15 proxy target 2.88–3.18); earlier generations failed the proportion proxy (**v1 = 4.70**, **v2 = 2.61**). v3 also *appears* to satisfy: bald head, blank face, plain grey short-sleeve tee, charcoal trousers, grey low sneakers, **no** wristbands / cargo pocket / orange zipper / green shoes, clean bare forearms. **This decision is NOT approval of the candidate, NOT promotion, NOT a new Master, NOT a runtime/base asset — only permission to review it formally.** **This is a NARROW review exception, NOT a repeal of D-033/D-042:** broad AI regeneration remains **risky and normally forbidden** (D-032/D-033/D-042 identity/proportion/style-drift risks all still apply — the heads-tall proxy is only ONE of many checks). **The full 164B.3 must verify — against `Northstar Master.png`, NOT `source-master.png` — much more than heads-tall:** silhouette, shoulder width, torso length, arm/hand geometry, leg length, style identity, line weight, head shape, ear style, outfit neutrality, skin consistency. **v3 may only proceed if the full 164B.3 PASSES or receives an owner-countersigned CONDITIONAL PASS.** **If the 164B.3 fails identity / style / geometry, v3 is REJECTED and remains an appearance-reference only.** **`Northstar Master.png` remains the canonical identity/style/coordinate datum (D-032).** v3 is copied into the **gitignored review area only** (`tools/avatar/build/phase2/d042-neutral-outfit-candidate-01/generated-base-review/`), **never committed as an asset.** **No approved mask or `Northstar Master.png` is changed; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.** Preserves D-032/D-033/D-042/D-043/D-044/D-045/D-046. (Evidence: heads-tall measurements v1/v2/v3 in the session's gitignored review area.) (2026-07-12) **[UPDATE 2026-07-13: full 164B.3 executed → v3 REJECTED (recommended FAIL; owner countersign pending).** Measured against `Northstar Master.png` on the shared 1024×1536 canvas: **silhouette IoU 0.7804** vs the **≥0.95** gate (and **0.8651** even bbox-normalised — so it is a genuine *shape* difference, not a fixable registration offset); **bald skull 456 px vs the D-032 anchor skull 384 px (2r, ctr 512,320 r≈192) = +18.8% oversized**; **shoulders −7.4%** (tolerance ±3%); figure height −8.3%. **The D-047 admission premise is void:** the heads-tall proxy divided v3's **bald** head into its body but the Master's **hair-inclusive** head mass (the crown is ~91 px: Master bbox top y=37 vs anchor skull top y=128; anchor bottom y=512 ≈ measured chin y=519) into *its* body — an apples-to-oranges basis that concealed an oversized skull. Against the real skull anchor the ratio is **3.52 (v3) vs 3.84 (Master)**. Two errors cancelled inside the ratio (head too big + body too short/narrow = bobble-head), which is exactly how a wrong figure lands on a "correct" heads-tall number. §3 style **FAIL** (two fields ≤2: soft airbrush/gradient shading vs Master's flat cel; bobble-head shape language, realistic inner-ear detail). §5 **FAIL** on D-032 by construction (v3 *is* a regeneration; not Master-derived geometry). §4 face-tone RGB Δ 14 (bar ≤5), though v3 is internally seam-free. **CONDITIONAL PASS unavailable** (requires §2 + §5 D-032 to pass; both fail, and skull mass / shoulder width / silhouette are not fixable without re-tracing geometry). **v3 remains an appearance reference ONLY — not promoted, not a Master, not an asset. `Northstar Master.png` remains the canonical identity/style/coordinate datum (D-032); D-033's no-broad-AI-regeneration stance is REINFORCED, not repealed.** **Transferable finding: the heads-tall proxy is RETIRED as an admission criterion** — it cancels compensating errors; future candidates must be screened on **raw-canvas silhouette IoU vs the Master** (v1=4.70 and v2=2.61 were correctly rejected, but v3 proves a *passing* proxy value carries no signal). The generated route did show that a broad generation yields a clean, seam-free, fully-neutral outfit in one shot (star, cargo pocket, orange zipper, green shoes, wristbands, elbow — all free) — but it cannot deliver **identity**, which is what actually gates Phase 2. **The real blocker is unchanged: the D-042 CPU candidate's elbow finish (D-045 option (b) GPU SDXL/Flux or (c) manual paint-over)** — a few-hundred-px touch-up on an identity-locked, protect-diff-0 base, a far shorter path than re-deriving identity from a generated figure. No approved mask or `Northstar Master.png` altered; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED. (Evidence: `v3-164b3-review.md`, `measure-generated-base.json`, `overlay-silhouette.png`, `overlay-head-anchor.png` — session's gitignored `generated-base-review/` area.)]** |
| **D-048** | **New `source-master.png` admitted as a REGISTERED DONOR for selected non-protected outfit/arm regions of the existing D-042 identity-locked candidate (2026-07-13).** A new hand-supplied `source-master.png` (1024×1536, RGBA) is permitted as a **donor pixel source** — and **nothing more** — for three regions of the D-042 candidate. **This is NOT a new base, NOT a new Master, NOT a promotion, NOT a runtime asset, NOT an approval of `source-master.png` as a whole, and NOT a repeal of D-032/D-033/D-042.** `Northstar Master.png` remains the canonical identity/style/coordinate datum (D-032). **Why the donor is credible (unlike the rejected v3, D-047):** it is the **same artwork**, not a regeneration — best alignment to the working candidate is **scale 1.0, translation-only (dx +24, dy −55)**; after that shift the **protect-zone silhouette IoU is 0.9899** and **85.2 %** of the identity zone is byte-identical or imperceptible (≤4). **Why it cannot be a base:** **protect-diff ≠ 0** — 9.8 % (11,355 px) of the protect zone is genuinely redrawn (**jaw+ears 41.2 %**, **hands+wrists 24.0 %**); the **shoes are still green** (21,629 green + 2,098 orange px); **scalp/hair remnants** persist (78 dark px above the skull, 796 in the scalp box). **Evidence that the donor beats the current candidate in the allowed regions:** the D-042 candidate carries visible compositing artifacts — a **patched flat torso rectangle** (hard paste seam **Δ122.9 @ y559**), the **orange sleeve stripes still present** (2,298 orange px), **green collar/armpit remnants** (1,906 green px), dirty sleeve/arm transitions, **trouser patch/block artifacts** (hard seam **Δ97.7 @ y902**), and an **underarm tone mismatch of ΔRGB 68 vs the hands** (the forearm does not even classify as skin: 6,766 / 4,703 non-skin px). The donor in the same regions: torso 77 green / 702 orange, seam Δ17.1; trousers 1 green, seam Δ27.9; **underarm ΔRGB 1 vs hands**. A full-area lift would import only **73 green + 188 orange** px. **ALLOWED donor regions (exhaustive): (1) torso/top incl. tee and sleeves, (2) trousers, (3) underarm/elbow (both arms).** **FORBIDDEN donor regions (exhaustive): the entire protect zone — head, scalp, hair remnants, ears, jaw, face, neck-in-protect, hands, wrists-in-protect — plus the SHOES, and anything outside the figure outline.** **Alignment rule: donor pixels are sampled at translation dx +24, dy −55 (same scale; translation-only). No rescaling, no rotation, no warp.** **Technical caveat — no naive full-region paste is allowed:** the donor's tee/trouser silhouette is **slimmer** than the current D-042 candidate's (whose tee is a recoloured *sweater* outline), so a straight lift would punch **11,681 coverage holes** (78.1 % within 3 px of the outline — closable by erosion; **1,066 px at 7–11 px depth = genuine shape divergence**; 39 interior). Donor masks must therefore be **eroded/restricted**, and the **residual-ring / deep-hole areas must remain pre-lift D-042 pixels or be separately reviewed**. **D-048 REQUIRES: protect-mask hard-excluded; shoes hard-excluded; composite ONLY inside the approved union donor mask; no approved mask changed; no `Northstar Master.png` change; no runtime change; no `assets/avatar-r2`; no `R2_MANIFEST`; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.** **VALIDATION required before ANY donor-lift may be accepted (all must hold): dimensions unchanged 1024×1536; protect-diff = 0; outside-union-mask changed = 0; donor coverage inside each import mask = 100 %; no imported transparent holes; imported green = 0 (except explicitly excluded residuals); imported non-skin orange = 0; underarm ΔRGB vs the frozen hands ≤ 5; silhouette outline unchanged vs the pre-lift candidate; shoe region byte-identical to the pre-lift candidate; visual overlay review before owner acceptance.** **Owner-acknowledged trade-off:** lifting torso + trousers + arms means the visible **outfit art becomes AI-authored**, while **identity (head/ears/jaw/hands) stays Master-derived and frozen** — this touches D-033's no-AI-for-base-art stance and is accepted **only** for non-identity outfit regions under the masks and validation above. **The donor-lift is NOT performed by this decision; this entry authorises the mechanism only.** `source-master.png` stays in the **gitignored review area** and is **never committed as an asset**. Preserves D-032/D-033/D-042/D-043/D-044/D-045/D-046/D-047. (Evidence: read-only region/coverage/alignment measurements in the session's gitignored review area.) (2026-07-13) **[AMENDMENT 2026-07-13 — corrected hole statistics; donor mechanism still valid, but currently BLOCKED by D-049.** During D-048 mask/overlay preparation the donor-mask builder was run and **superseded the hole statistics quoted above**, which were wrong: they came from a **BFS distance bug** (pixels beyond the search cap kept `dist = -1` and were mis-bucketed as distance 0–1, i.e. counted as "outline band"). **The "78.1 % of holes within the 3 px outline band" and "1,066 px deep holes" figures in this entry are VOID.** **Corrected, measured with a full uncapped BFS:** total donor-coverage holes **11,894**; only **3,896 px (32.8 %)** sit in the 0–3 px outline band; **7,998 px remain after the 4 px erosion**; the **deepest hole is 74 px** from the outline. **Shape of the residual (better than feared):** it is **not** a global ring — it is **two vertical strips along each arm, roughly y700–y1100**, where the donor's slimmer arm does not cover the candidate's; **torso and trousers have practically no residual.** **Donor masks built (deterministic, non-AI, review-only, gitignored):** union **203,307 px** (torso/tee **92,622**, trousers **92,524**, underarm **18,161**), outline band kept **9,059 px**, residual **7,998 px**. **All D-048 guards on the union were clean: union ∩ protect = 0; union ∩ shoes = 0; imported green = 0; imported non-skin orange = 0.** **BUT the donor-lift is currently INEFFECTIVE on the torso/sleeve defects:** it would remove only **1.6 %** of the orange sleeve stripes and **3.9 %** of the green sweater remnants — because those pixels sit **inside the protect mask** (see **D-049**). **D-048 remains valid as a donor MECHANISM, but NO donor-lift may proceed until D-049 is resolved**, and the D-048 donor masks must then be **regenerated against the corrected protect mask**. No mask edited, no composite performed, no image committed; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.]** |
| **D-049** | **Protect-mask contamination correction — REQUIRED before D-048 can be effective (2026-07-13).** **Finding:** the current identity-lock `protect-mask.png` **contains clothing pixels**. Audited against the D-042 candidate in the correct frame (115,660 protect px total): **legitimate skin/identity 92,521 px (80.0 %)**, **orange sleeve stripes 8,220 px (7.1 %)**, **green sweater remnants 2,462 px (2.1 %)**, line art 6,429 px (5.6 %), other 6,028 px (5.2 %). **10,682 px of GARMENT is currently inside the identity-lock.** The mask appears to have been derived by **skin-colour detection**, and the orange sleeve stripes — whose hue is skin-adjacent — were swept in. **Consequence:** the orange stripes and green remnants survived every earlier pass **not because they are true identity, but because the protect mask is contaminated**. While this mask stands, **no donor-lift, no masked inpaint and no manual paint-over can ever remove them** (protect-diff = 0 forbids touching them). **This is the same class of defect as D-046**, where the wristbands survived because of mask logic (a mask gap), not because of genuine identity — here it is the inverse (wrongly *inside* the mask). **Decision:** produce a **review-first, deterministic (non-AI) protect-mask carve proposal** — `protect-mask-v2` — that removes clothing-coloured pixels from the protect mask while preserving true identity. **PRESERVE: head, blank face, ears, jaw, neck skin, hands, and all real skin/identity line art. REMOVE from protect: orange sleeve stripes, green sweater remnants, non-identity garment blobs on the arms. DO NOT remove real skin.** **Required review artifacts before ANY mask is accepted:** current protect-mask overlay · contaminated-pixels overlay · proposed `protect-mask-v2` overlay · removed-from-protect mask · kept-identity mask · a report with pixel counts · **explicit proof that hands/face/ears/jaw remain protected** · **explicit proof that the orange/green clothing pixels are removed from protect**. **Required process:** **no mask replacement until the owner approves the overlay**; the **old protect-mask remains available** (never deleted/overwritten); **`protect-mask-v2` is review-only until countersigned**; **after approval, the D-048 donor masks must be regenerated against `protect-mask-v2`**. **Any subsequent donor-lift must still pass:** protect-diff = **0** against the chosen protect mask · outside-union changed = **0** · shoe region unchanged · no imported green/orange · dimensions unchanged (1024×1536). **No mask is edited by this decision; it authorises the review-first carve proposal only. `Northstar Master.png` untouched (D-032); no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED. Nothing promoted.** Preserves D-032/D-033/D-042/D-043/D-044/D-045/D-046/D-047/D-048. (Evidence: protect-mask audit + donor-mask builder report in the session's gitignored review area.) (2026-07-13) **[AMENDMENT 2026-07-13 — contamination figure CORRECTED (10,682 → 2,848); carve proposal built; carve overlay OWNER-APPROVED; mask NOT yet replaced.** **(1) The "10,682 px of garment inside the identity-lock" figure in this entry is WRONG and is superseded.** It was produced by classifying garment **by colour on the RECOLOURED candidate**, where the **cel-shadow ramp on SKIN** (blue channel ≈ 37–55) falls inside the same RGB range as the sleeve-stripe orange (blue ≈ 3) — so shaded skin on the head, ears, jaw and hands was counted as "garment". **Colour alone cannot separate deep skin shadow from stripe orange; their ranges overlap.** **(2) Corrected method (now the method of record):** classify against the **ORIGINAL assembled base** (`base-assembly/base-assembled-review-v1.png` — the green star sweater, where garment is unambiguous), **plus a SPATIAL guard**: an orange pixel counts as garment **only if sweater-green lies within 8 px** of it (the star, sleeve stripes and collar trim sit *inside* the sweater; skin shadow on the head has no green anywhere near it). **The spatial guard rescued 3,271 skin-shadow pixels** that the colour-only rule would have carved out of the identity-lock. **(3) CORRECTED contamination: 2,848 px** — **green garment 540 px** (collar, wristband edge) + **orange garment 2,308 px** (sleeve stripes, collar trim). The **core D-049 claim stands** — the orange sleeve stripes *were* wrongly inside the identity-lock, which is why they survived every pass — **but the magnitude was overstated 3.75×**. Composition of the current protect mask, measured on the ORIGINAL: **skin 93,775 · line art 8,022 · other (kept) 7,744 · green garment 540 · orange garment 2,308**. **(4) Carve proposal built (deterministic, non-AI, review-only, gitignored):** **`protect-mask-v2` = 112,812 px** (was 115,660); **removed 2,848 px**. **Proofs: P1 real skin removed = 0 · P2 identity line art removed = 0 · P3 head/face/ears/jaw 85,864 → 85,863 (1 px) · neck+collar 6,543 → 5,659 (884) · arms 9,000 → 7,105 (1,895) · hands/wrists 13,103 → 13,035 (68) · P4 garment left inside v2 = green 0 / orange 0.** The **68 px in the hand band are the green WRISTBAND edge** — exactly D-046's "65 px protected residual"; removing it from protect is intended (it is garment, not identity). The 1 px in the head band is a single stray pixel at the collar boundary. **(5) OWNER-APPROVED (2026-07-13): the carve overlay was reviewed and countersigned** — the removed set is exactly the **orange sleeve stripes + collar/shoulder trim + wristbands**, and head, ears, jaw, neck, forearms and hands remain fully protected. **(6) NOT YET DONE — no mask has been replaced.** The **old `protect-mask.png` is retained and byte-unchanged** (sha256 `0ED0820D…`); **`protect-mask-v2` remains review-only** until the swap is executed as its own step; **the D-048 donor masks MUST be regenerated against `protect-mask-v2`** before any donor-lift, and that lift must still pass protect-diff = 0 · outside-union = 0 · shoe region unchanged · no imported green/orange · dims 1024×1536. No approved mask replaced; `Northstar Master.png` untouched; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.]** |
| **D-050** | **Arm/torso fringe resolved by ADOPTING THE DONOR SILHOUETTE — and the fringe turns out to be a D-042 candidate geometry DEFECT, not a donor problem (2026-07-13).** The D-048 donor-lift (executed review-only, all hard guards clean: protect-diff 0, outside-union 0, shoe region unchanged, imported green/orange 0, donor coverage 100 %, underarm **ΔRGB 68 → 1** vs the frozen hands, i.e. **164B.3 §4 closed**) left a **pale seam along the inner edge of each arm** — ~12,125 px, avg 7.8/6.6 px wide, worst 19 px. **Root cause (proven by full-row silhouette runs, not inferred):** the **D-042 candidate's arms are FUSED to the torso** (one unbroken opaque run, e.g. row 940 = x 310–710), while **`Northstar Master.png` has SEPARATED arms** (row 880 = left arm 320–368 · torso 378–634 · right arm 649–700) — and the **donor matches the Master's arm topology to within 1–2 px** (row 880 = 320–367 · 379–632 · 650–700). The candidate's arm/torso gap was **filled in by the D-042 AI-inpaint passes** (the same context-bleed failure mode D-045 documented). **The fringe was therefore the candidate's own geometry defect showing through once the donor laid the correct arm on top — not a donor artifact.** **Decision: adopt option (a) — DONOR SILHOUETTE.** Body pixels that the donor does not cover (the arm/torso gap + thin outer slivers) are **erased to transparent**, restoring the arm/torso separation. Deterministic, non-AI; **protect zone and shoes untouched (protect-diff 0, shoe changed 0, changed-outside-target 0)**. **Option (b) — deterministic nearest-colour fill — is REJECTED:** it propagates colour inward from both sides of the gap and produces a **streaky, smeared band** (visibly worse than the fringe). **Known residual / open question:** the gap **onset row** does not match the Master exactly — Master opens at **y 854**, donor/variant-(a) at **y 711**, the current D-042 candidate at **y 1035**. Variant (a) is thus **closer to the Master than the present candidate** (143 px off vs 181 px off, opposite directions), and the y 711–854 band is where the outfit legitimately differs (Master wears a **long-sleeve sweater** whose sleeve touches the torso; the neutral base wears a **short-sleeve tee**, so the bare, thinner arm separates higher). **Whether that silhouette difference passes 164B.3 §2 (silhouette IoU ≥ 0.95 vs Master) is NOT settled by this decision — the review must decide it.** Other known residuals after the lift: a **thin dark-green hairline along the collar/shoulder edge**, small orange flecks at the arm/torso joins, and ~312 px of neck skin-shadow previously miscounted as orange. **Nothing is promoted:** the lifted/variant-(a) candidate stays in the **gitignored review area**; no asset committed; no approved mask changed; `Northstar Master.png` untouched (D-032); no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; **Gate 2 NOT satisfied; Gate 3 PAUSED; 164B.3 NOT run.** Preserves D-032/D-033/D-042/D-043/D-044/D-045/D-046/D-047/D-048/D-049. (Evidence: donor-lift validation report, fringe A/B report, full-row silhouette-run comparison Master vs donor vs candidate — session's gitignored review area.) (2026-07-13) |

| **D-051** | **164B.3 executed on the D-050 candidate → CONDITIONAL PASS, owner-countersigned (2026-07-13).** The full 164B.3 base-coherence review was run on `d042-outfit-candidate-d050-CURRENT.png` (D-048 donor-lift + D-049 protect-mask-v2.1 + D-050 donor silhouette) against `Northstar Master.png` (D-032). **Verdict: CONDITIONAL PASS — owner-countersigned.** **§4 PASSES FOR THE FIRST TIME — this is the headline.** The pre-lift candidate failed §4 outright (forearm 199,180,163 = **ΔRGB 68** from the frozen hands, plus a band seam at the elbow). The donor-lift closes it: forearms **254,189,122 / 254,189,124 vs hands 254,188,123 → ΔRGB 1, no seam.** **§2 PASS (owner-confirmed reading):** shoulders **+0.8 %** (tol ±3 %); **hand centroids land on the Master's** (left **exact** 345,1009; right 3 px); **no vertical stretch**; and **below y854 the arms match the Master to 1–2 px** (row 880 — Master 320–368 | 378–634 | 649–700 · candidate 320–367 | 379–632 | 650–700). **Silhouette IoU below-seam: raw 0.9458 (misses the ≥0.95 gate by 0.0042); DRAPE-EXCLUDED 0.9594 (passes).** The deficit is **garment-driven, not body-driven**: the **y701–860 arm-gap band alone is 41 % of all mismatch** (the Master's long sweater sleeve touches the torso; the mandated **short-sleeve tee** leaves a bare, thinner arm that separates), and the mandated **grey low sneakers vs the Master's green chunky shoes** add another 19 % — **60 % of the mismatch is outfit, not geometry**. **The worksheet specifies "Silhouette IoU (body minus drape)", and the OWNER HAS CONFIRMED that reading**, so §2 is scored on the drape-excluded figure → **PASS**. **§3 PASS** (all fields ≥4). **§6 PASS** (no Frankenstein seam; the donor-arm → frozen-hand junction is tonally continuous, ΔRGB 1–2 — exactly what the pre-lift candidate could not achieve). **§5 CONDITIONAL — owner has countersigned the D-032 row:** the **outfit art is donor-authored (AI)**, registered to Master geometry within 1–2 px, while **identity (head/face/ears/jaw/hands) stays Master-derived with protect-diff = 0** — the trade-off explicitly accepted in D-048 and now formally countersigned here. **MUST-FIX PUNCH-LIST (downstream layers stay BLOCKED until cleared and re-verified):** **(1) the WRISTBAND SHAPE GHOST at both wrists** — the colour is gone but the **contour is still legible as a shape** (tonally clean: ΔRGB 1–2, shading SD 22.1 vs the forearm's 27.4, so it is *not* a flat repaint); this is D-046's known "faint band mark", now more visible against the clean donor arm, and it is **the one true §5 signature-element failure**; **(2) thin dark-green collar hairline (~281 px)** along the collar/shoulder edge; **(3) ~574 px of real garment orange** (sleeve-stripe remnants) at the arm/torso joins; **(4) arm halo — ~1,016 near-white edge px** along the newly opened arm/torso gap (the other ~1,791 near-white edge px are the **white sneaker soles**, legitimate art — an earlier count of 23,077 "halo" px was wrong and is void). **OUT OF SCOPE for this candidate:** the **bald skull measures 322 px vs the D-032 anchor's 384 px (−16.1 %)**; this is **inherited unchanged from the D-043 base** (the pre-lift candidate measures the same 322) and was **not introduced by D-048/D-049/D-050** — raise it against **D-043** separately if it is considered a defect. **Nothing is promoted.** The candidate stays in the **gitignored review area**; no asset committed; no approved mask changed; `Northstar Master.png` untouched (D-032); no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; **`AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.** Preserves D-032/D-033/D-042/D-043/D-044/D-045/D-046/D-047/D-048/D-049/D-050. (Evidence: `164b3-d050-review.md`, `164b3-d050-measurements.json`, IoU band breakdown, wristband tone/flatness measurements — session's gitignored review area.) (2026-07-13) **[CORRECTION 2026-07-13 — punch-list item (1), the "wristband shape ghost", is VOID. It is NOT present in the D-050 candidate; §5 is correspondingly STRONGER than D-051 states.** **Reviewer error:** the wristband observation was made on **`lift-wrist-left-after.png`** — the **intermediate donor-lift v1 image, taken BEFORE the D-050 donor-silhouette step** — not on the candidate the review was actually about (`d042-outfit-candidate-d050-CURRENT.png`). A side-by-side of the two at 6× settles it: in **lift-v1** the wristband protrudes as brown blobs on both sides of each wrist; in the **D-050 candidate those blobs are GONE** — **D-050 (variant a) erased them**, because they lay outside the donor's slimmer arm silhouette and were therefore donor-transparent. **What actually remains at the wrist:** **199 px of dark contour remnants** (thin marks at the arm edge) and a **soft ~8-luminance tone step** where the donor's arm art meets the frozen protect-zone wrist skin. **No wristband shape is legible.** **Why the tone step is NOT a defect:** **62 % of the wrist area lies inside the protect zone** and is frozen — but those frozen pixels are **legitimate identity (the base's own wrist skin), not D-046's flat recolor**. D-046 hard-excluded protect, so its recolour lay *outside* protect and has already been replaced by donor art. The step is therefore the seam between **donor arm art** and **frozen Master-derived skin**, both legitimate, with **ΔRGB 1–2** between them. **Explicitly NOT done:** carving further protect to lift the remaining 62 % — that would **cut real identity out of the lock on a false premise**, exactly the class of error the whole D-049 investigation existed to prevent. The wrist protect region **stays**; after D-049 there is **no garment contamination left there**. **Consequence: the punch-list drops from four items to three** — (2) the thin dark-green collar hairline (~281 px), (3) ~574 px of real garment orange at the arm/torso joins, (4) the arm halo (~1,016 near-white edge px) — all of which stand unchanged. **§5's one claimed signature-element failure is withdrawn**; the neutral outfit carries **no legible signature element**. Verdict remains **CONDITIONAL PASS** (owner-countersigned); the §2 drape reading and the §5 D-032 countersign are unaffected. No mask changed; `Northstar Master.png` untouched; nothing promoted; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED. (Evidence: `wrist-liftv1-vs-d050.png` 6× A/B, wristband footprint/ghost analysis, wrist union-membership counts — session's gitignored review area.)]** |

| **D-052** | **Collar hairline cleared — D-051 punch-list item (2) is DONE (2026-07-13).** The **dark-green collar/shoulder hairline** on the D-050 candidate has been removed by a **deterministic, non-AI donor-pixel copy** restricted to the **collar/shoulder band (y 500–600)**. Output: **`d042-outfit-candidate-d052-collar-cleanup.png`** (sha `E19980D9…`), **review-only, gitignored, NOT promoted.** **Method — ground truth, not colour heuristics:** residue was verified against the **ORIGINAL assembled base** (where the sweater is unambiguously green) **plus the aligned donor**, never by colour on the recoloured candidate alone. This is the rule adopted after the D-049/D-051 misclassifications, and it held: of the **1,136 px that merely *read* dark-green on the candidate**, only **699 were verified sweater residue** — **0 px of neck/head skin or shadow were misclassified** (the failure mode that burned earlier passes), **278 px were identity line art** and **159 px were not garment on the original**, all correctly rejected; **0 px were inside protect**. **Scope honoured:** of the 699 verified residue px, **555 lay in the collar/shoulder band and were corrected (495 changed)**; the remaining **144 px are small green specks down at the arm/torso joins and were deliberately LEFT ALONE** — out of scope for this task. **Alignment:** donor sampled at **dx +24, dy −55, scale 1.0** — no rescale, no rotation, no warp. **Validation (all guards passed):** dims 1024×1536 · **protect-diff = 0** · **outside-correction-mask changed = 0** · **shoe region changed = 0** · **head/face/ears/jaw changed = 0** · **alpha changed = 0 (silhouette not altered)** · donor coverage inside mask **100 %** · transparent donor px imported **0** · **uncorrected sweater residue = 0**. **Reviewer self-correction (recorded so the number is not misread later):** the first validation run reported "84 px residue remaining". **That was a defect in the TEST, not in the correction.** Those 84 px are **byte-identical to the donor** — they are the **donor's own tee COLLAR SEAM**, a thin, slightly green-tinted stitch line (RGB ≈ 38,64,47) that is part of its art. The test had re-verified corrected pixels against the *sweater's* ground truth, which is meaningless: a pixel now holding the donor's colour **is** donor art. The check was redefined to count only **uncorrected old pixels** → **0**. Whether the donor's green-tinted collar seam is aesthetically acceptable is an **owner judgement**, not a residue defect; it reads as a normal seam line, not as sweater remnant. **Punch-list status: item (2) CLEARED. TWO items remain** — (3) **~574 px of real garment orange** at the arm/torso joins, and (4) the **arm halo (~1,016 near-white edge px)**. **No mask changed** (protect stays v2.1, sha `302324B7…`); `Northstar Master.png` untouched (D-032); **no 164B.3 re-run**; nothing promoted; no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`; **Gate 2 NOT satisfied; Gate 3 PAUSED.** Preserves D-032/D-033/D-042…D-051. (Evidence: `collar-cleanup-report.json`, `collar-analysis.json`, `collar-before-after.png`, `collar-correction-{mask,overlay}.png` — session's gitignored review area.) (2026-07-13) |

| **D-053** | **Outfit punch-list CLEARED — item (3) fixed, item (4) reclassified as an inherited D-042/D-043 alpha/matte artifact (2026-07-13).** **Gate 2 is NOT closed by this decision; a final 164B.3 re-verification is still required.** **(1) ITEM (3) — garment residue at the arm/torso joins: CLEARED.** **D-051's "~574 px of real garment orange" figure is WRONG** — like the wristband item, it was measured on **donor-lift v1**, not on the actual **D-050/D-052** candidate. **The real verified garment residue on the D-052 candidate was 36 px.** A **180 px review mask** was produced: **36 px were genuine old garment residue and were changed**; the other **144 green-looking px were already byte-identical to the donor** and are therefore **donor art, not residue** (they were the "144 green specks" D-052 deliberately left out of scope — that framing is hereby corrected: they were never residue). **The orange-looking marks still visible on the arms are donor-authored fold/shadow artwork (≈175 px), not sweater remnants.** Output review candidate: **`d042-outfit-candidate-d053-arm-residue.png`** (sha `2CB93EE0…`), **gitignored, NOT promoted.** Method: deterministic, non-AI donor-pixel copy at **dx +24, dy −55, scale 1.0**, verified against the **ORIGINAL assembled base**. **All guards passed: protect-diff = 0 · outside-mask changed = 0 · shoes changed = 0 · head/face/ears/jaw changed = 0 · alpha changed = 0 (silhouette preserved) · uncorrected verified garment residue = 0.** **Methodological finding worth keeping:** a first run reported **18,460 px of "verified residue"** — the candidate-side orange test did not exclude **skin**, and on the ORIGINAL that same bare arm was the **green sweater SLEEVE**, so the ground-truth check confirmed "garment" and the whole arm was flagged. **The ground-truth guard is necessary but NOT sufficient:** the arm was legitimately garment then and is legitimately skin now — the candidate-side test must independently recognise what is still wrong. **(2) ITEM (4) — the "arm halo": RECLASSIFIED, NOT EDITED.** **D-051's description ("arm halo caused by the newly opened arm gap") is incorrect.** The pale edge is a **global 1-px white-antialias fringe along arms, torso AND legs**. **Lineage proves it is inherited, not introduced:** pre-lift D-042 candidate **2,461 px** · donor-lift v1 **2,461 px** · D-050/D-052 **2,011 px** — **the donor process REDUCED the halo by 450 px; it did not create it.** **2,006 of the 2,011 current halo px correspond to transparent background in the ORIGINAL base** (white-matte blending from the D-042/D-043 pipeline), and **1,557 of 2,011 lie INSIDE the protect zone**. **Consequently: fixing only the 454 non-protect px is REJECTED** (it would leave an inconsistent, half-cleaned edge — worse than leaving it); **fixing all 2,011 px is FORBIDDEN without a separate owner-approved protect/alpha decision** (it would break protect-diff = 0, the hard rule enforced throughout this track). **The halo is therefore MOVED OUT of the outfit punch-list and into a separate inherited D-043 alpha/matte cleanup track.** **No halo edit performed; no protect change.** **(3) STATUS.** **The OUTFIT punch-list is now CLEARED** (item 1 void per the D-051 correction; item 2 fixed in D-052; item 3 fixed here; item 4 reclassified out). **This does NOT promote the candidate. This does NOT mark Gate 2 as a final PASS.** **A targeted final 164B.3 re-verification on the D-053 candidate is still REQUIRED before Gate 2 can be closed.** Nothing promoted; no approved mask changed (protect stays **v2.1**, sha `302324B7…`); `Northstar Master.png` untouched (D-032); no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; **`AVATAR_R2` stays `false`; Gate 2 NOT satisfied; Gate 3 PAUSED.** Preserves D-032/D-033/D-042…D-052. (Evidence: `arm-residue-report.json`, `arm-defects-analysis.json`, `arm-residue-before-after.png`, halo lineage counts across the four candidates — session's gitignored review area.) (2026-07-13) |

| **D-054** | **Skull audit on the D-053 candidate → verdict A: INVALID MEASUREMENT. The "−16.1 % undersized skull" finding is VOID (2026-07-13).** **VOID WORDING (authoritative):** **"The finding 'bald skull 322 px vs D-032 anchor 384 px = −16.1 %' is hereby declared VOID. It is not an apples-to-apples measurement and it does not document a geometry defect in the D-053 candidate."** **(1) WHY THE MEASUREMENT IS INVALID.** The **322 px** is a real **crown-to-chin** measurement on the D-053 art. The **384 px** is merely **2 × r192** taken from the **legacy D-004 SVG anchor** — `viewBox 160×240`, head `cx80 cy50 r30`, upscaled **×6.4**. **It was never measured on `Northstar Master.png`.** The project's own documentation labels it a **"first-guess"** requiring **"cross-check detected head bbox"** and **human confirmation** (`docs/164k-anchor-mask-extraction-plan.md`, `docs/164b-asset-production-plan.md` §0.3) — **that cross-check was never carried out.** Comparing a measured crown-to-chin distance against the **diameter of an unvalidated legacy circle** is a category error. **(2) THE LEGACY ANCHOR IS FALSIFIED.** The anchor makes a testable prediction — **eye line at y ≈ 301** (centres (435,301)/(589,301)). **Measured on the Master, the actual eye centres are (434, 374) and (580, 371)** — an error of **≈ 70–73 px**. The same doc table already flagged the legacy eye box as wrong ("anime eyes are larger than the legacy box", **"must measure"**); the **head radius was never confirmed either**. **The circle is therefore NOT a valid coordinate datum for the Master's head or crown.** *(Shifting it down ~71 px to meet the Master's real eye line places its crown at y ≈ 199 — essentially the D-053 crown at y 201; the radius may be roughly right, the position is not.)* **(3) ACTUAL D-053-vs-MASTER HEAD GEOMETRY — NO DEFECT.** **Left ear bbox matches within ≤1 px** (Master 327,340,400,482 · D-053 327,340,400,483); **right ear bbox within ≤1 px** (Master 630,340,703,459 · D-053 630,340,703,460); **head width at the ears = 377 px in BOTH**; **chin 519 vs 523 (4 px)**; **protect zone vs the D-043 assembled base: 89,269 px identical, 0 different (byte-frozen)**. **There is no measured head/identity geometry defect in D-053.** **(4) ACKNOWLEDGED MEASUREMENT GAP (not a defect).** The **Master's true bald crown does not exist as data** — it is hidden under the hair (Master figure top y=37 is a **hair spike**, not a cranium). The D-053 crown was **reconstructed during D-043**, and **no ground truth exists in the repo to verify it against**. This is an **epistemic gap, not evidence of an error**, and the legacy anchor cannot fill it. **RULE: heads-tall and skull-height may NOT be used again unless the SAME landmark definition is applied to BOTH images** (the D-047 failure — a bald head compared against a hair-inclusive one — must not recur). **(5) NEW TECHNICAL FINDING — THE REAL OUTCOME OF THIS AUDIT.** **The D-032 head anchor must be RE-DERIVED from `Northstar Master.png`.** Until that is done, **the legacy anchor is NOT authoritative** for **face · eyes · blink · hair · headwear · any other head-registered layer** — all of which currently register to a circle that is **~71 px off**. **This is a Gate 3 / downstream ANCHOR BLOCKER.** **It does NOT block the neutral D-053 base and it does NOT block Gate 2.** **(6) GATE STATUS.** **D-054 does NOT close Gate 2.** **Gate 2 now awaits only the separate owner decision on the inherited §7 alpha/matte halo (D-053).** **Gate 3 remains PAUSED and is now additionally blocked by the need to re-derive the D-032 anchor.** No image edited; no mask changed; no new geometry introduced; nothing promoted; `Northstar Master.png` untouched; D-053 candidate unchanged (sha `2CB93EE0…`); no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`. Preserves D-032 as the **image** datum (the Master itself is unaffected; only the **legacy anchor numbers** derived from D-004 are challenged). Preserves D-033/D-042…D-053. (Evidence: `d053-skull-audit.md`, `d053-skull-audit-report.json`, `d053-vs-d032-skull-overlay.png`, `d053-vs-master-head-overlay.png`, `skull-landmarks.png`, `skull-difference-map.png` — session's gitignored review area.) (2026-07-13) |

| **D-055** | **Anchor conflict resolved — D-054's "Gate 3 anchor blocker" is WITHDRAWN; the existing human-calibrated anchors are AUTHORITATIVE and are now independently confirmed (2026-07-14).** **(1) D-054 CORRECTION — reviewer error.** D-054 claimed that *"face, eyes, blink, hair, headwear currently register to a circle that is ~71 px off"* and raised a **Gate 3 / downstream ANCHOR BLOCKER**. **That claim is FALSE and is hereby withdrawn.** A full search of the codebase shows the legacy anchor `(512,320) r≈192` appears **ONLY** in (a) this session's gitignored review scripts and (b) the **§2 text of `docs/164b3-base-review-worksheet.md`**. **It is used nowhere in the production tooling and nowhere in the frontend runtime.** **`tools/avatar/extract-anchor-masks.mjs` carries a separate, human-calibrated anchor block** — **164L.4 `MANUAL_ANCHOR_OVERRIDES`** + **164S eye semantics** (manual 6× visual calibration) + **164T eye-box / glasses recalibration** — explicitly marked *"These are the authoritative anchor geometry for the Master."* **The downstream layers were never mis-registered. The Gate 3 anchor blocker did not exist.** **(2) INDEPENDENT CONFIRMATION of the existing anchors.** An automated re-derivation from `Northstar Master.png` (dark-mass detector constrained to require adjacent white sclera, so eyebrows and hair fringe are rejected) **agrees with the human 164S/164T calibration**: measured left iris centre **(432, 378)** vs 164S **(432, 387)** — **x matches to the pixel**; right **(582, 373)** vs 164S **(577, 387)** — x within 5 px; measured eye bboxes **390,342→465,427** and **543,338→627,424** sit a few px inside the 164T boxes **378,336→476,436** and **531,336→629,436**. **Two independent methods — one human, one automated — agree. The existing eye anchors are correct and do NOT need re-deriving.** **(3) THE LEGACY EYE LINE IS EVEN WORSE THAN D-054 SAID (but harmless).** The real eye line is **y ≈ 386–394** (164S iris y 387, pupil y 393–394), not y 301 — an error of **~90 px**, not 75. **It is harmless because nothing uses it.** **(4) NO NEW HEAD ANCHOR IS INTRODUCED.** A head ellipse was derived during this audit (centre (508,362), rx 151, ry 161, crown y 201 from the D-053 bald head) — **it is retained as EVIDENCE ONLY and is NOT adopted as an anchor.** Adding it would create a **competing anchor that nothing consumes** and would contradict verified human calibration. **There is no head-centre/radius anchor in the production contract at all**; head-registered work uses `faceMaskRegion`, `headwearRegion` and the 164T eye boxes. **(5) THE ONE REAL FIX — the worksheet.** `docs/164b3-base-review-worksheet.md` §2 is amended: the legacy anchor line is **removed and marked VOID**, the authoritative anchor source is named, and the **D-054 rule is restated** — *head/body ratio, heads-tall and skull height may only be scored when the SAME landmark definition is applied to BOTH images; the Master's bald crown is not measurable and no datum may be invented for it.* This was the **only** place the legacy numbers could do harm, and it is where the bogus "−16.1 % skull" came from. **(6) METHOD NOTE (two self-corrections during this derivation, recorded so they are not repeated).** A first pass was **contaminated twice**: (a) stray dark specks left of the head shifted the measured head centre to x 465 (fixed by restricting all head measurements to the connected component containing the face → centre x **508**); (b) a naive dark-pixel eye detector swallowed the **eyebrows and hair fringe**, inflating the eye box to 107×111 px and dragging the centroid upward (fixed by requiring adjacent white sclera). **Neither contaminated figure was carried forward.** **(7) GATE STATUS.** **Gate 3's anchor blocker is REMOVED** (it was never real). **Gate 3 remains PAUSED** for its own reasons. **Gate 2 is unaffected and still awaits only the owner decision on the inherited §7 alpha/matte halo (D-053).** No image edited; no mask changed; no anchor value changed in code; nothing promoted; `Northstar Master.png` untouched; D-053 candidate unchanged (sha `2CB93EE0…`); no runtime / `assets/avatar-r2` / `R2_MANIFEST` change; `AVATAR_R2` stays `false`. Preserves D-032/D-033/D-042…D-054 (D-054 stands except for its point (5), withdrawn here). (Evidence: `d032-anchor-v2-proposal.json`, `anchor-v2-on-master.png`, `anchor-v2-on-d053.png` — session's gitignored review area; `tools/avatar/extract-anchor-masks.mjs` lines 36–127.) (2026-07-14) |

| **D-056** | **GATE 2 CLOSED — D-053 passes final 164B.3 with an owner-accepted inherited §7 alpha/matte exception (2026-07-14).** **(1) APPROVED GATE-2 CANDIDATE.** `tools/avatar/build/phase2/d042-neutral-outfit-candidate-01/donor-lift/d042-outfit-candidate-d053-arm-residue.png` (sha256 **`2CB93EE0…`**). **Lineage:** D-043 base assembly → **D-048** donor lift → **D-049** protect-mask-v2.1 → **D-050** donor silhouette → **D-052** collar cleanup → **D-053** arm/torso residue cleanup. **(2) FINAL 164B.3 VERDICT: PASS WITH OWNER-ACCEPTED INHERITED §7 ALPHA/MATTE EXCEPTION.** **§2 PASS** — shoulders **+0.8 %**; hand centroids match the Master closely (left exact 345,1009; right 3 px); **no vertical stretch**; arms match the Master **within 1–2 px below y854**; **owner-confirmed drape-excluded silhouette IoU = 0.9594** (worksheet wording: "silhouette IoU (body minus drape)"). **§3 PASS.** **§4 PASS** — forearm **ΔRGB vs the frozen hands improved from 68 → 1**; **no elbow/skin seam** (this section failed outright pre-lift). **§5 PASS** — **0 px uncorrected garment residue**, **0 px garment residue frozen inside protect**, **no legible signature clothing elements**. **§6 PASS.** **§7 — ACCEPTED AS AN OWNER-APPROVED INHERITED LEGACY EXCEPTION. The halo is NOT technically removed and must never be recorded as removed.** **(3) HALO DECISION — the facts, recorded.** The pale edge is a **global ~1 px white-antialias / matte fringe along arms, torso AND legs**. **It is not an arm-gap artifact and was NOT introduced by D-048/D-050.** **Lineage: pre-lift D-042 = 2,461 px · donor-lift v1 = 2,461 px · D-050/D-053 = 2,011 px — the donor process REDUCED it by 450 px.** **2,006 of the 2,011 current halo px correspond to transparent background in the ORIGINAL base**; **1,557 of 2,011 lie INSIDE protect.** **OWNER DECISION: do NOT perform a partial 454 px non-protect cleanup** (it would leave an inconsistent half-cleaned edge); **do NOT modify all 2,011 px**; **do NOT carve or weaken protect**; **ACCEPT the halo as inherited D-042/D-043 alpha/matte TECHNICAL DEBT.** **It is no longer a Gate 2 blocker.** **It may only be revisited through a separate owner-approved alpha/protect decision.** **(4) SUPERSEDED FINDINGS — none of these block Gate 2.** **D-051 wristband ghost: VOID** (corrected — it was observed on the intermediate donor-lift v1 image, not on the D-050 candidate). **Collar hairline: CLEARED (D-052).** **"~574 px orange residue": WRONG** — an intermediate-image count; the actual residue was **36 px**, cleared by **D-053**. **"−16.1 % undersized skull": VOID (D-054)** — the 384 px "anchor" was a legacy D-004 SVG circle (×6.4), never measured on the Master. **D-054's Gate 3 anchor blocker: WITHDRAWN (D-055)** — the legacy circle is used nowhere in tooling or runtime. **The existing 164L.4 / 164S / 164T human-calibrated anchors remain AUTHORITATIVE** (independently confirmed in D-055). **(5) STATUS TRANSITION.** **Final 164B.3 verdict: PASS WITH OWNER-ACCEPTED INHERITED §7 ALPHA/MATTE EXCEPTION.** **GATE 2: SATISFIED / CLOSED.** **D-053 is the owner-approved Gate-2 neutral base candidate.** **Gate 3: remains PAUSED** until an explicit owner command starts it. **`AVATAR_R2`: remains `false`.** **(6) BOUNDARIES — what closing Gate 2 does NOT mean.** It does **NOT** mean runtime promotion · **NOT** copying the candidate into `assets/avatar-r2` · **NOT** changing `R2_MANIFEST` · **NOT** enabling `AVATAR_R2` · **NOT** starting Gate 3 · **NOT** fixing the halo · **NOT** changing protect · **NOT** replacing `Northstar Master.png`. **The D-053 file remains in the gitignored review area until promotion is separately authorised.** No image edited; no mask edited (protect stays **v2.1**, sha `302324B7…`); no new measurements; nothing promoted; `Northstar Master.png` untouched (D-032); no runtime / `assets/avatar-r2` / `R2_MANIFEST` change. Preserves D-032/D-033/D-042…D-055. (Evidence: `164b3-d053-final-review.md`, `arm-residue-report.json`, halo lineage counts across the four candidates — session's gitignored review area.) (2026-07-14) |

| **D-057** | **D-053 Gate-2 neutral base PROMOTED to a tracked source asset — SOURCE-ONLY, NOT RUNTIME ACTIVE (2026-07-14).** **(1) TRACKED SOURCE.** Path: **`assets/avatar/reference/neutral-base-v1-gate2-d053.png`** · **SHA-256 `2CB93EE00BE89D16D1B1E9CAE9781B596F931A52FD6F667238C9E9BDA38AFE4B`** · **1024 × 1536** · **331,506 bytes** · **alpha channel preserved**. It is a **byte-identical copy** of the D-053 review candidate — verified three independent ways (SHA-256 equality, a full 331,506-byte comparison with 0 differing bytes, and `fc /b` "no differences encountered"); **no re-encoding, no resize, no metadata normalisation, no PNG optimisation, no image processing of any kind.** **The original review file is preserved untouched** in the gitignored review area (`tools/avatar/build/phase2/d042-neutral-outfit-candidate-01/donor-lift/d042-outfit-candidate-d053-arm-residue.png`, same SHA-256). The destination follows the existing precedent for an owner-approved base-layer source — `recovery-base-v1-blankface.png` (D-043) already lives in the same directory — and its name deliberately does **NOT** match the runtime naming pattern `body-{body_type}-{skin_tone}-v{n}` used under `assets/avatar-r2/`, so it cannot be mistaken for a runtime asset. **(2) PROVENANCE.** **D-043** base assembly → **D-048** donor lift → **D-049** protect-mask-v2.1 → **D-050** donor silhouette → **D-052** collar cleanup → **D-053** arm/torso residue cleanup → **D-056** Gate-2 approval → **D-057** tracked source promotion. **(3) APPROVAL STATUS.** **Final 164B.3: PASS WITH OWNER-ACCEPTED INHERITED §7 ALPHA/MATTE EXCEPTION.** **Gate 2: SATISFIED / CLOSED** (D-056). **D-053/D-057 is an owner-approved neutral base SOURCE.** **`Northstar Master.png` remains the canonical identity/style/coordinate datum (D-032) — this asset does NOT replace it.** **`protect-mask-v2.1` remains unchanged** (sha `302324B7…`). **The §7 halo is STILL PRESENT and is accepted as inherited technical debt — it is NOT removed, and must never be recorded as removed.** **(4) RUNTIME BOUNDARY — explicit.** This is a **tracked SOURCE asset, NOT runtime active**. **No change to `assets/avatar-r2`. No change to `R2_MANIFEST`. No change to imports or runtime asset paths.** `R2_BASE_PATH` is `/assets/avatar-r2` and `R2_MANIFEST` is a hard-coded literal in `js/avatar-layers.js` — a file placed in `assets/avatar/reference/` **cannot register itself** and **no runtime code references that directory**. **`AVATAR_R2` remains `false`. Gate 3 remains PAUSED / NOT STARTED. This promotion does NOT start Gate 3 and does NOT change the current Phase-1 rendering.** Preserves D-032/D-033/D-042…D-056. (2026-07-14) |

| **D-058** | **Gate-2 protect-mask-v2.1 promoted to a tracked source-only identity contract; byte-identical, NOT runtime active (2026-07-14).** **(1) TRACKED CONTRACT ASSET.** Path: **`assets/avatar/reference/gate2-protect-mask-v2.1.png`** · **SHA-256 `302324B7B9C0ACB124C982294D1B85FEB9EDBF467AAAD0F0A52B2C7E96F692F5`** · **1024 × 1536** · **8-bit RGBA PNG (color type 6)** · **13,715 bytes** · **byte-identical copy** of the gitignored review/build file `tools/avatar/build/phase2/mask-refinement/d049-carve/protect-mask-v2.1.png` — verified by SHA-256 equality, equal byte count and a full 13,715-byte comparison with **0 differing bytes**; **no re-encoding, no resize, no metadata normalisation, no PNG optimisation, no colour/alpha change, no image processing of any kind.** **The original gitignored source file is preserved untouched.** **(2) PROVENANCE.** **D-049** owner-approved protect-mask-v2.1 (deterministic carve; delta report: 2,246 garment px removed from protect, 0 skin px, 0 neutral line-art px, head/face/ears/jaw and hands/wrists bands unchanged) → used as the binding identity-lock through **D-050** (donor silhouette), **D-052** (collar cleanup) and **D-053** (arm/torso residue cleanup), each pass holding **protect-diff = 0** against the owner-approved Gate-2 candidate → **D-056** Gate-2 closure → **D-057** tracked neutral-base source → **D-058** tracked protect-contract promotion. **(3) CONTRACT STATUS.** This is the **tracked, durable copy of the ACTIVE Gate-2 protect contract**. The mask's **bytes and meaning are unchanged**; this is **NOT a new mask version**, grants **no new writable regions**, and **neither weakens nor extends protect**. **Changing the mask still requires a separate owner decision** (per D-049 process). The **D-057 base and `Northstar Master.png` remain unchanged** (D-057 sha `2CB93EE0…`, Master sha `2CA10EF8…`). **The §7 halo remains accepted inherited technical debt and is untouched** (1,557 of its 2,011 px sit inside protect — clearing it would break protect-diff = 0; see D-056). **(4) RUNTIME BOUNDARY — explicit.** **Source-only, NOT runtime active.** No change to `assets/avatar-r2`, no change to `R2_MANIFEST`, no change to `js/avatar-layers.js`, no imports and no runtime asset paths reference this file. **`AVATAR_R2` remains `false`. Gate 3 remains PAUSED / NOT STARTED — D-058 does NOT start Gate 3.** Preserves D-032/D-042…D-057. (2026-07-14) |

| **D-059** | **Neutral R2 base alpha fringe PARTIALLY remediated at runtime-asset level — trousers/shoes cleared, arms only ~34 % reduced; PARTIAL and TEMPORARY, the permanent fix belongs in the source art (2026-07-20; scope corrected 2026-07-20 after post-merge visual verification — see (11)).** **(1) SYMPTOM.** The promoted neutral R2 base showed a light/grey halo on dark backgrounds along the forearms, hands, trouser outer sides and shoe outlines. **(2) MEASURED CAUSE — three contributions, not one.** **(a) 655 FULLY OPAQUE near-white matte pixels** painted into the artwork on the silhouette's outer rim — the dominant contribution; **no alpha-domain operation can reach them**. **(b) 1,047 light PARTIAL-ALPHA pixels** in the antialias band. **(c) 85 px of DETACHED white debris** in 24 components floating 1–3 px off the arms. **This is the same family as the D-056 §7 inherited alpha/matte halo, now measured at the runtime-asset level.** **(3) WHY THE EARLIER ATTEMPT FAILED — recorded so it is not repeated.** A first pass (`build-r2-alpha-decontaminate.mjs`) repaired only RGB of pixels with `a < 255`. It rewrote ~33,900 pixels with **zero visible change**: it could not touch (a) at all, and for (b) it **sampled the still-white opaque rim and propagated the matte straight back out**. **Pass order is load-bearing** — the opaque rim must be cleaned *before* the antialias band is decontaminated. **(4) METHOD.** `tools/avatar/build-r2-alpha-rim-fix.mjs` — deterministic, **non-AI**, four passes (opaque matte rim recolour → partial-alpha decontamination → detached-debris clear → `a=0` RGB decontamination), encoded **`-lossless -exact`**. The light test is **relative to the adjacent body colour**, so genuinely light artwork (white shoe soles, laces) is left untouched. **No erosion, no dilation, no blur, no new outline.** **(5) VERIFIED against the re-decoded WebP (hard-fail invariants).** Main silhouette pixel set **unchanged** (`mainPxDiff=0`) · main silhouette alpha **unchanged** (`mainAlphaDiff=0`) · interior RGB **unchanged** (`mainInteriorRgbDiff=0`) · alpha writes **85 px, exclusively detached debris** · dimensions **512×768** · **determinism**: two independent encodes byte-identical · **idempotent**: re-running on the promoted asset finds `matteRimPx=0, partialAA=0, detached=0`. **(6) CHANGED ASSET.** `assets/avatar-r2/base/body-neutral-medium-v2.webp` — **old** SHA-256 `0DEA234893361A1D143636178643C3BD8119F1861F398438654E980BD8AC7182` · **23,594 bytes** → **new** SHA-256 `3A30D8C7BC29A4813E9F4F2902FED26235B3458A56F733C11067559968DA4F37` · **66,526 bytes**. **(7) ENCODING DEVIATION — deliberate and measured.** The `167a` promotion contract's lossy `-q 90` path was **measured to alter 27,526 opaque pixels per re-encode generation**, which would silently undo the repair. **`-lossless -exact` is the only encoding that keeps every non-repaired pixel byte-exact.** Cost: **23.0 KB → 65.0 KB (+42 KB)** — to be weighed against the ADR-163D ~350 KB total-avatar budget before any further layer receives the same treatment. **(8) NOT CHANGED.** `face-neutral-v1` (30 matte rim px) and `eyes-neutral-fixed-v1` (46 px) were **measured but deliberately left unchanged**: stacked on the repaired base their rim sits over opaque skin, not against the background, and shows **no separate visible fringe**. **(9) BOUNDARIES.** **`AVATAR_R2` remains `false`.** **No runtime, `R2_MANIFEST`, blink, database or golden changes.** `Northstar Master.png` (D-032), the D-057 base source and the D-058 protect contract are **untouched** — this repair was applied to the **runtime WebP only**, not to any source or countersigned Gate-2 artefact. **(10) STATUS — PARTIAL AND TEMPORARY.** This is a **runtime remediation, not a source fix**, and it is **incomplete** (see (11)). The matte rim is **painted into the source art**; the durable correction belongs in the master raster (167a track) and remains later work. **The original `167a` Gate-3 countersign and its `0DEA2348…` hash are PRESERVED as historical record** — see the note in `docs/167a-phase2-gate3-neutral-asset-promotion.md` §3. **(11) SCOPE CORRECTION — the fringe is NOT fully removed (recorded 2026-07-20, post-merge).** A visual verification of the merged asset on all four real surfaces under the R2 opt-in (fixture-intercepted; no backend contact) showed a **still-visible light fringe along both forearms and hands**. Measured light pixels (`luma > 200`) in the promoted asset vs the original: **trousers (y 560–640): 115 → 0 (cleared)**; **arms (y 330–560): 1,565 → 1,038 (only ~34 % reduced)**. **ROOT CAUSE of the shortfall:** `RIM_DIST = 2` made only the outermost two pixel rings eligible, so matte structures **thicker than ~4 px keep an "interior"** that the rim pass treats as legitimate body colour — and that slab's own outer ring is then compared against its own white interior, giving `Δluma ≈ 0`, so it is not flagged either. Depth histogram of the promoted asset: **779 light px at d ≤ 2** (eligible) vs **26,093 at d > 2** (never eligible; most of that total is the legitimately white shoes). **Thin matte (trouser sides, shoe outlines, hand contours) was therefore fully repaired; the thick matte slabs beside the arms survived.** **Sections (1)–(9) remain factually accurate as written** — every invariant, count and hash still holds, and the change is an unambiguous improvement with no regression, no new outline and no shape change. **What is corrected here is the SCOPE claim only:** this decision must not be read as "the alpha fringe is removed". **A complete fix requires a different detection strategy** (component-based identification of matte slabs outside the silhouette, not a rim-distance test) **and is deliberately NOT attempted here** — the owner decision is to let the residual stand and close it at source in the 167a master-raster work. Preserves D-032/D-033/D-042…D-058. (PR #96) (2026-07-20) |

| **D-060** | **Permanent source-art fix of the neutral R2 arm fringe is BLOCKED by the D-058 protect contract — a protect-diff = 0 fix is not possible; escalated as a separate owner protect/alpha decision (2026-07-21).** This is the D-059 §11 follow-up: attempt the durable fix at the SOURCE (not the runtime WebP). **Outcome: FAIL at the protect gate — no source candidate was built, no protect exception was created.** **(1) MANDATE.** Re-measure the D-059 residual arm/hand fringe at source scale, project it against the D-058 protect contract, and build source candidates ONLY if a meaningful protect-diff = 0 fix is possible. **(2) FILES (all verified byte-identical to their register hashes; none modified).** Northstar Master `assets/avatar/reference/Northstar Master.png` (sha `2CA10EF8…607A21`, 1,192,451 B, D-032) · D-057 neutral base source `assets/avatar/reference/neutral-base-v1-gate2-d053.png` (sha `2CB93EE0…8AFE4B`, 331,506 B) · D-058 protect contract `assets/avatar/reference/gate2-protect-mask-v2.1.png` (sha `302324B7…F692F5`, 13,715 B) · runtime base `assets/avatar-r2/base/body-neutral-medium-v2.webp` (sha `3A30D8C7…8DA4F37`, 66,526 B, D-059). **(3) RESIDUAL MEASUREMENT (source 1024×1536, arm band y 660–1180).** Deterministic, non-AI detector — NOT luma-alone: a pixel counts only if it is `alpha>0`, on the outer silhouette edge (`distToTransparent ≤ 4`), reads light on the dark app bg (`compLuma > 200`), AND is `> 40` luma lighter than the nearest interior body colour it borders (so skin, grey tee and dark trousers each set their own local reference; legit mid-tone art and the white shoes are excluded). **Total visible arm residual = 3,488 px.** By category `[total / of-which-inside-protect]`: skin-edge 14/13 · tee/sleeve-edge 961/885 · hand/wrist 858/664 · detached-debris 409/408 · legit-highlight 0/0 · uncertain 1,246/1,235. **Physical basis:** only `alpha>0` pixels render, so the visible fringe is the silhouette's own light antialias/matte rim — not invisible fully-transparent void pixels. **(4) PROTECT OVERLAP — the decisive finding.** **3,205 of 3,488 residual px (91.9 %) lie INSIDE the protect zone; only 283 px (8.1 %) are outside** (282 main-figure edge + 1 detached; outside-protect distribution underarm 77 / wrist 18 / hand 188). This is the same conflict D-056/D-058 recorded for the GLOBAL halo (2,011 px, 1,557 = 77 % inside protect), now measured specifically for the arms and found **even more locked (91.9 %)**. **(5) WHY A protect-diff = 0 FIX IS NOT POSSIBLE.** A protect-diff = 0 change can touch at most the 283 outside-protect px (8.1 %); cleaning only those leaves **91.9 % of the visible fringe untouched** — exactly the "inconsistent, half-cleaned edge" the owner explicitly REJECTED in D-056 (where the analogous 454-px non-protect-only cleanup was forbidden). Per the binding rule (D-049/D-058) no pixel inside protect may change without a separate owner decision, and this investigation is forbidden from creating a protect exception itself. **Therefore: FAIL. No A/B/C source candidate built; no "best" candidate selected (fail-closed).** **(6) BASELINE CONFIRMED (render scale, §3).** Reproduced on all four real surfaces under the R2 opt-in (fixture-intercepted; 0 backend calls): the arm/hand fringe is still visible at 320 px, fades at 64, effectively gone at 48/32; **trousers and shoes remain clean — no regression since D-059**; no page/console errors. **(7) RECOMMENDATION — owner decision required.** The durable fix needs the separate protect/alpha decision D-056 deferred. Options: **(a)** approve a narrow protect exception for the arm-edge antialias band (only path to a source-level clean; needs a new D-decision); **(b)** ACCEPT the residual permanently — visible only at large sizes, invisible at 48/32, and `AVATAR_R2 = false` means no user sees it (lowest risk, recommended now); **(c)** defer to a full 167a master-raster redraw where protect is re-derived from scratch (right long-term home). Not chosen here — this entry records the conflict and stops for owner review. **(8) OWNER-REVIEW PACK (gitignored).** `tools/avatar/build/source-arm-fringe-owner-review/` — `00-contact-sheet.png`, `src-protect-overlay.png`, `residual-mask-overlay.png` (red = inside protect / green = outside), `protect-overlap-overlay.png`, `residual-mask.png`, `zoom-src-{Lforearm,Lhand,Rforearm}.png`, `surface-{avatar,hub,quiz,shop}-page.png`, `baseline-sizes-{dark,light}.png`, `baseline-arm-zooms.png`, `manifest.json`, and five deterministic non-AI analysis tools. **(9) BOUNDARIES — nothing touched.** No image edited; **protect mask unchanged** (sha `302324B7…`); **D-057 source unchanged** (sha `2CB93EE0…`); **Master unchanged** (sha `2CA10EF8…`); **runtime WebP unchanged** (sha `3A30D8C7…`); no candidate promoted or committed; no `R2_MANIFEST`, runtime, blink, database or golden change; **`AVATAR_R2` stays `false`; Gate 3 remains PAUSED.** Preserves D-032/D-033/D-042…D-059. (branch `fix/avatar-r2-source-arm-fringe`) (2026-07-21) |

| **D-061** | **Neutral R2 arm fringe fixed under a NARROW, OWNER-APPROVED protect write-exception — Candidate B promoted (source + runtime). D-058 protect is untouched and NOT generally weakened (2026-07-21).** This resolves the D-060 protect conflict by the owner's chosen path (a): a precisely-scoped, temporary write-exception for the arm/wrist/hand matte only. **(1) OWNER APPROVAL.** After visual review of the owner-review pack, the owner approved **Candidate B** as the accepted arm-fringe fix, and explicitly accepted the documented residual (see (8)). **(2) THE CONFLICT IT RESOLVES.** D-060 measured that 91.9 % of the visible arm fringe lies inside the D-058 protect contract, so no protect-diff = 0 fix was possible; the owner then granted a narrow write-exception rather than a general protect weakening. **(3) WRITE-EXCEPTION (new tracked source-only contract).** Path `assets/avatar/reference/arm-fringe-write-exception-v1.png` · **SHA-256 `47DBEC44A7EF6C3B…`** · **1024×1536** · **2,353 px** (connected 1,982 + detached 371) · segments forearm 1,658 / wrist 618 / hand 77 · bbox left x304–377 y703–1155, right x632–716 y700–1119. It is the intersection of (measured residual) ∩ (arm/wrist/hand outer-edge near-white matte, luma ≥ 215) ∩ (D-058 protect), gated to the arm columns and excluding face/head/ears/jaw/hair/eyes/torso/inner-arm/fingers/nails/skin-shadow/line-art/tee-highlights. **It is a NARROW write-exception for THIS fix only; it does NOT replace, edit or weaken D-058, which remains byte-identical** (sha `302324B7…`). **(4) METHOD (deterministic, non-AI).** `tools/avatar/build-r2-arm-fringe-fix.mjs` (tracked; hash-locks its D-057/D-058/D-059 inputs and hard-fails on any mismatch or invariant breach; writes production assets only with `--promote`). Connected matte → RGB recoloured to the nearest interior SKIN colour (region-aware BFS from skin only; never samples matte/tee/trouser); detached matte → alpha cleared (not the main figure, not erosion). **Main-figure alpha is unchanged → no erosion, no shape change, no new outline; line-art, fingers and identity untouched.** **(5) NEW TRACKED SOURCE.** `assets/avatar/reference/neutral-base-v2-armfringe.png` · **SHA-256 `347A258FB962DB38…`** · **1024×1536** · **328,373 B** · owner-approved arm-fringe source built on the D-057 base. **The historical D-057 source `neutral-base-v1-gate2-d053.png` is preserved byte-identical** (sha `2CB93EE0…`); this is a NEW version, not a rewrite. Source verify: **alphaMainDiff = 0 · protect-diff outside exception = 0 · outsideExceptionDiff = 0 · deterministic (two builds byte-identical).** **(6) RUNTIME PROMOTION.** `assets/avatar-r2/base/body-neutral-medium-v2.webp` — **old** `3A30D8C7…8DA4F37` · 66,526 B (D-059) → **new** `28765EEA616DD92B…` · **67,174 B** (`-lossless -exact`). Built as the CURRENT D-059 runtime + the arm-exception fix (NOT re-derived from source), so **D-059's trouser/shoe result is preserved byte-exact**: only the 376 arm-exception px changed (`outsideMaskDiff = 0`), main-figure alpha unchanged, lossless roundtrip pixel-identical, deterministic. Budget: 67.2 KB base layer, within the ADR-163D ~350 KB total-avatar budget. **(7) RENDER VERIFICATION.** All four surfaces (avatar/hub/quiz/index/shop) under the R2 opt-in (fixture-intercepted, 0 backend) served the promoted asset **byte-identical to the approved Candidate B** (`28765EEA…` on every surface). The clear white matte along the forearms/wrists/hands is removed/reduced as in Candidate B; no new dark outline; no shape or identity change; bukser and sko unchanged from D-059. **(8) ACCEPTED RESIDUAL — this result must NOT be described as "completely removed" or "perfect".** The owner accepted that faint sub-215 specks and a minor light patch at the wrist/trouser transition may remain; Candidate B is visually approved as SUFFICIENT for this phase, not flawless. A fully-clean result would need a wider exception or the 167a master-raster redraw (later work). **(9) BOUNDARIES.** **`AVATAR_R2` stays `false`.** No `R2_MANIFEST`, runtime-code, blink, database, golden or R2-activation change. `Northstar Master.png` (D-032), the D-057 source and the D-058 protect contract are all byte-identical. The write-exception applies only to this fix and grants no general writable region. Preserves D-032/D-033/D-042…D-060. (branch `fix/avatar-r2-arm-fringe-protect-exception`) (2026-07-21) |

| **D-062** | **Atomic R2 asset-load fallback — a failed mandatory R2 layer now drops the WHOLE avatar to a complete C2 render (closes activation-audit F1) (2026-07-22).** **(1) THE F1 GAP.** The activation-readiness audit found that `composeR2Layers`/the manifest gate chose R2 BEFORE any image loaded, so a 404/decode failure on a mandatory R2 layer left a **broken partial R2 stack** (broken base `<img>` + floating face/hair), never a C2 fallback. **(2) FIX — atomic preload/decode gate (`js/avatar-render-c2.js`, `mountC2Avatar`).** When R2 is chosen, every MANDATORY layer (base, blush, face, iris, eyes-fixed, hair) is **preloaded + `.decode()`-ed off-DOM** before the stack is made visible. On ANY mandatory failure (HTTP 4xx/5xx, `img.onerror`, `decode()` reject, `naturalWidth===0`, empty/invalid URL, missing manifest entry after the R2 choice), the whole stack **atomically falls back to the complete existing C2/SVG path** — never a partial R2 stack, never a broken image, never a visible base-without-face / face-without-base window (the prior render stays until the complete new stack is ready). **(3) OPTIONAL vs MANDATORY.** Safe cosmetic overlays (aura/back) are OPTIONAL: a failed overlay is dropped individually and the R2 base survives — it does NOT collapse the base to C2. Documented + unit-tested. **(4) RACE / ABORT (§5).** A per-element mount generation (WeakMap) is claimed synchronously at call start; after every await the mount checks it still owns the element and returns `"aborted"` without touching the DOM if a newer mount superseded it. A stale/aborted load can never overwrite a newer render; no double C2 mount; no duplicate blink layer. **(5) BLINK / LIVING CONTRACT.** `mountC2Avatar` now returns the render path actually mounted (`"r2"|"c2"|"aborted"`) and stamps `rootEl.dataset.avatarRenderPath`. `blinkConfigFor(identity, r2ActiveOverride?)` takes an optional override; each surface (avatar.html, hub.html, app.js) keys BOTH the expression-overlay gate and the blink profile off the ACTUAL mounted path — so an R2 asset-load fallback gets the C2 living contract (expression ON, C2 lids), never R2 lids on a C2 base. Blink starts only after the complete mount; the fallback does not create a duplicate blink layer (engine's single `#avatar-blink-layer`, `destroy()` clears its timer). **(6) LOGGING (§9).** One controlled, non-sensitive `console.warn` per failed mount (reason only — no identity, student-id or token); no repeat loops. **(7) TESTS.** New `tests/unit/avatar-r2-atomic-fallback.test.mjs` (14 cases: all-load→R2, base/face/eyes/hair fail→C2, decode-failure→C2, multi-fail→one C2 mount, optional-overlay-drop keeps base, newer-mount-wins abort, forceC2, null root, blinkConfigFor override). New `tests/avatar-r2-fallback.spec.ts` (fixture-intercepted, self-served, 0 backend contact): clean→R2 and base-404→C2 / face-404→C2 on avatar/hub/index — passing on Chromium + Firefox + WebKit. `npm run test:unit` 69/69; existing avatar/living/golden suites unchanged (C2 default byte-for-byte identical). **(8) BOUNDARIES.** **`AVATAR_R2` stays `false`** — this is fallback hardening, not activation. No asset, `R2_MANIFEST`, database, migration, RLS, Edge Function, golden or pilot change. Only render-path code (`js/avatar-render-c2.js`, `js/avatar-layers.js` blinkConfigFor signature, avatar.html/hub.html/app.js engine gates) + two test files. **Closes F1 only; F2–F5 from the audit remain open.** Preserves D-032/D-033/D-042…D-061. (branch `fix/avatar-r2-atomic-load-fallback`) (2026-07-22) |

| **D-063** | **Deterministic R2 blink-frame goldens — open/closed lids are now visually protected on exact R2 neutral-medium (closes activation-audit F5). Also fixes a latent D-062 regression that stopped avatar.html blink from mounting (2026-07-22).** F5 flagged that R2 blink was unit-tested and mounted but its OPEN vs CLOSED lids were never visually compared or golden-protected. **(1) DETERMINISTIC TEST SEAM.** `BlinkEngine.forceFrame('open'|'closed')` (`js/avatar-blink-engine.js`): clears any pending auto-blink, builds the lid layer if absent (so it works under prefers-reduced-motion, which otherwise suppresses it), pins both lids to a STATIC transition-free frame, and sets the layer's inline `display:block` to override the reduced-motion `#avatar-blink-layer{display:none}` CSS. It uses THIS engine's mode geometry/fill, so a golden captures the engine's REAL R2 lids (Option-A geometry `cx 66.7/90.6, cy 60.3, rx 8.91, ry 9.06`; measured medium fill `#FEC183`), not a replica. The constructor self-registers `window.__avatarBlinkEngine` as a test handle **GATED behind an explicit `window.__AVATAR_TEST__ === true` signal** (set by the harness via `addInitScript`); **production never sets that flag, so no blink-engine global is exposed to users.** `destroy()` clears the global when it still points at the destroyed engine, and a remount overwrites it with the new engine. **Both are test-only: nothing calls them in production, so auto-blink timing and user-facing behaviour are unchanged** (reduced-motion users still see no blink). *(Gate added as a pre-merge review fix — pre-merge review flagged the ungated global as SHOULD-BE-GATED; no golden regeneration needed, the render output is byte-identical.)* **(2) LATENT D-062 REGRESSION FIXED.** The atomic R2 preload (D-062) delays when `mountC2Avatar` appends its layers; on avatar.html `initAvatarLifeEngines()` ran at top level right after `await loadAll()` (which fires the async render without awaiting), so it saw no layers yet and bailed → **the blink/expression/presence engines were never created on avatar.html**. Fixed by initialising the engines at the end of `renderAvatarC2()` once its layers exist (guarded against double-init, mirroring hub.html), with the top-level call kept as a guarded fallback. hub.html/app.js already init post-mount and were unaffected. **(3) GOLDENS (chromium; win32 + linux).** `tests/avatar-r2-blink-frames.spec.ts` — SELF-SERVED + FIXTURE-INTERCEPTED (0 backend, no shared-student mutation; uses the branch code that carries the seam, which production does not yet have). Baselines: `r2-avatar-page-blink-open`, `r2-avatar-page-blink-closed`, `r2-hub-avatar-blink-closed`, `r2-quiz-avatar-blink-closed`. Determinism: reduced motion freezes breathing/expression + suppresses auto-blink, leaving only the pinned lid frame. **(4) CONTRACT VERIFIED (all browsers where functional).** open→closed→open with, at each step: exactly one blink layer, exactly two lids (no duplicate eyes), correct scaleY(0)/scaleY(1), R2 fill + Option-A geometry on close, no C2/R2 mixing, no broken images, and unchanged container/base/hair/face bounding boxes (no position/scale shift, no seam). open-before and open-after both match the SAME open baseline within tolerance (no residue). `destroy()` removes the lid layer; a C2 fallback (dark skin under opt-in) uses C2 lids (`cx 68/92`), never the R2 geometry. **(5) TESTS.** Golden captures chromium-only; DOM/functional + destroy + C2-fallback-lids + the test-handle gate check run on Chromium + Firefox + WebKit (12/12 incl. the gate test). `npm run test:unit` 69/69; existing R2 goldens (D-062/F4) + avatar-living + hub-avatar-living unchanged; the 8 blink baselines are byte-identical before/after the gate fix. Added to the manual Linux golden-regen workflow. **(6) BOUNDARIES.** **`AVATAR_R2` stays `false`** — visual test coverage + a blink-init fix, not activation. No asset, `R2_MANIFEST`, database, migration, RLS, Edge Function or pilot change. Only `js/avatar-blink-engine.js` (seam), `avatar.html` (engine-init timing fix), the new spec + baselines, the regen workflow, and this doc. **Closes F5; F2 (reproducer idempotency) and F3 (Phase-2 doc refresh) remain open.** Preserves D-032/D-033/D-042…D-062. (branch `test/avatar-r2-blink-frame-goldens`) (2026-07-22) |

| **D-064** | **Phase-2 pilot/runtime docs refreshed to the current decomposed-stack reality (closes activation-audit F3). Docs-only (2026-07-23).** F3 flagged that the pilot-onboarding and flag docs still described **Phase-1** (a single baked **PNG** base with a **static** face), which would mislead a pilot operator today — the runtime is now the **Phase-2 decomposed WebP stack** (base `body-neutral-medium-v2.webp` + face + eyes + iris + blush + hair) with **live blink** (Option-A lids, D-061/D-063) and breathing; only dynamic expressions remain off (fixed neutral raster face). **(1) REFRESHED (docs only):** `docs/167a-phase1-pilot-rollout.md` — retitled to "Avatar R2 Pilot Rollout", §1/§3/§4/§6/§7 updated for the WebP decomposed stack + live blink + D-062 atomic fallback + D-061 accepted arm-fringe debt; the 2026-07-01 Phase-1 verification is kept as **historical** and a 2026-07-22 audit re-verification (Phase-2, fixture-intercepted) added. `docs/157r-feature-flags.md` — the `AVATAR_R2` row updated (WebP stack + live blink + atomic fallback, no longer "temporary PNG preview"). `docs/AVATAR_SYSTEM.md` §9 — an additive current-status note ("Phase-2 SHIPPED under opt-in") placed above the retained Gate-progression history. **(2) ELIGIBILITY UNCHANGED:** the pilot criteria (neutral-medium only; aura/back cosmetics only) remain correct and are preserved. **(3) NOT TOUCHED:** the historical/decision docs (D-register, 167a decomposition/step-3/master-asset plans, phase-2 asset brief, ROADMAP, the `assets/avatar-r2/README.md`) correctly describe the Phase-1→Phase-2 journey **as history** and were left as-is; no historical decision rewritten. **(4) BOUNDARIES:** **docs-only** — no code, asset, `R2_MANIFEST`, database, golden, test or `AVATAR_R2` change; **`AVATAR_R2` stays `false`**. **Closes F3. F2 (arm-fringe reproducer idempotency) is now the SOLE remaining open activation-audit finding; F1/F4/F5 are closed (D-062/D-063 + R2 goldens).** Preserves D-032/D-033/D-042…D-063. (branch `docs/f3-phase2-pilot-refresh`) (2026-07-23) |
| **D-065** | **Arm-fringe reproducer made idempotent/verifiable post-promotion (closes activation-audit F2 — the LAST open finding). Tool-only, no asset change (2026-07-23).** F2 flagged that `tools/avatar/build-r2-arm-fringe-fix.mjs` aborted on `main` (`exit 4`, "input hash mismatch: D-059 runtime `28765eea…`"): its runtime input-hash gate was locked to the **D-059 pre-fix** blob (`3a30d8c7…`), but `main` already carries the **D-061 promoted output** (`28765eea…`, 67,174 B). So the promoted runtime could no longer be re-derived or verified from a fresh clone without supplying the pre-fix blob from history. **(1) FIX (tool-only):** the runtime gate now accepts **either** the D-059 input **or** the D-061 output — the runtime is legitimately both the tool's input and its output. From the D-059 input it re-derives the Candidate-B fix as before; from the already-promoted D-061 output the **runtime-side selection mask is empty and the runtime candidate touches 0 px** (the arm matte is already recoloured to skin, luma < 215, so nothing re-selects), and the lossless re-encode reproduces the on-disk file **byte-for-byte**. The owner-approved **source-side exception mask is unchanged (still 2353 px, SHA `47dbec44…`)** and is reproduced byte-for-byte — it is derived from the D-057 source, not from the runtime, so the tool still legitimately prints `exception mask: 2353 px`. **(2) STRONGER SELF-VERIFICATION:** added hard-fail invariants asserting the three derived artefacts equal the tracked D-061 outputs byte-for-byte (`arm-fringe-write-exception-v1.png` `47dbec44…`, `neutral-base-v2-armfringe.png` `347a258f…`, runtime `28765eea…`) plus an explicit idempotency check (re-run over the promoted runtime must touch 0 px). Any blob that is neither the D-059 input nor the D-061 output is still rejected. **(3) VERIFIED:** on `main` the tool now runs green (`exit 0`, "idempotent verify mode"); `--promote` re-writes all three artefacts **byte-identically** → `git status assets/` stays **clean** (proven: SHAs unchanged before/after). The D-058 protect PNG and D-057 source PNG remain untouched. Provenance intact (D-061 recorded the byte-identical reproduction at promotion time; the tool can now reconfirm it from `main`). **(4) BOUNDARIES:** only `tools/avatar/build-r2-arm-fringe-fix.mjs` changed — **no asset, `R2_MANIFEST`, database, migration, RLS, Edge Function, golden, runtime-JS or pilot change; `AVATAR_R2` stays `false`.** The gitignored audit scratch (`tools/avatar/build/…`) and the vendored encoders were not committed. **Closes F2. ALL activation-audit findings F1–F5 are now closed (D-062/D-063/D-064/D-065 + R2 goldens); F6 is accepted debt, F7/F8 are LOW.** Preserves D-032/D-033/D-042…D-064. (branch `fix/avatar-r2-reproducer-idempotent`) (2026-07-23) |
| **D-066** | **Path-aware Playwright CI — skip the full browser suite on docs-only and standalone avatar-tool-only PRs, fail-closed to full. CI-config only (2026-07-23).** After the R2 audit closed, the last several PRs were docs-only or build-tool-only yet each paid the full ~20-min (sometimes 60-min-timeout) live-backend Playwright suite for zero runtime risk. **(1) THREE MODES (pull_request only):** a tracked pure classifier `.github/ci/classify-changes.mjs` maps the PR's changed files to exactly one mode — **docs** (every file under `docs/**`), **avatar-tool** (≥1 file under `tools/avatar/**` AND every file under `tools/avatar/**` or `docs/**`), or **full** (everything else). docs → `git diff --check` + mojibake scan; avatar-tool → those + `node --check` on changed `tools/avatar/*.mjs|*.js`; full → the existing `npm ci` → browser install → global setup → whole Playwright suite, **unchanged**. **(2) SAFETY NET:** any `push` to main is **forced `full`** (classification never skips main-CI); changes to `.github/workflows/**`, `package*.json`, `playwright.config.*`, `tests/**`, `js/**`, `assets/**`, `supabase/**`, `*.html`, `*.css` — anything outside the two safe prefixes — are `full`. **(3) FAIL-CLOSED:** non-array/empty/malformed file list, an unknown path, or any classifier/import error resolves to **`full`** (over-classifying is always safe; under-classifying never is). **(4) REQUIRED CHECK PRESERVED:** the single job is still named **`test`**, still always runs and always reports in every mode (conditional *steps*, never a skipped job), so a branch-protection `test` requirement needs **no change** and a docs PR cannot be blocked by a missing/`skipped` check. **(5) IMPLEMENTATION:** official `actions/github-script@v7` paginates `pulls.listFiles` (handles >100-file PRs), imports the classifier, sets `steps.classify.outputs.mode`, logs the changed files, and writes a job summary (event/commit/files/mode/checks/skipped/reason) — **no new third-party changed-files action**. The classifier unit test (`tests/unit/ci-classify-changes.test.mjs`, 10 spec cases + fail-closed edges) runs in **every** mode, proving the fast-mode logic even on this full-mode PR. **(6) NOT CHANGED:** no app runtime, HTML/CSS, assets, goldens, database, RLS, Edge Functions or existing browser tests; timeout/retries/concurrency/secrets unchanged; **`AVATAR_R2` stays `false`.** The live-backend/concurrency split (fast modes still queue on `e2e-shared-supabase`) is intentionally **deferred to a later PR**. This PR itself edits `.github/workflows/**` → classifies as **full** and runs the whole suite one last time. Preserves D-032/D-033/D-042…D-065. (branch `ci/path-aware-playwright`) (2026-07-23) **· Follow-up fix (2026-07-24, branch `fix/ci-avatar-tool-eof`):** the avatar-tool `node --check` step used a bash `while read` loop that silently dropped the **last** file when the changed-files list had no trailing newline (`files.join('\n')` for a 1-file PR) — so a tool-only PR classified `avatar-tool` correctly and skipped Playwright, but syntax-checked **0** scripts (observed "passed on 0 changed tools/avatar script(s)" on PR #108). Fixed by (a) newline-terminating the list in the github-script writer and (b) rewriting the check in Node (`split('\n')` + `execFileSync('--check')`, like the mojibake scan), immune to the missing-newline case. Verified locally: 1 script checked both with and without a trailing newline. Workflow-only; classification rules, modes, fail-closed behaviour and the `test` required check are unchanged; `AVATAR_R2` stays `false`. |
| **D-067** | **Fast CI modes run OUTSIDE the shared Supabase concurrency lock (extends D-066). CI-config only (2026-07-24).** Problem: `playwright.yml` had **workflow-level** `concurrency: e2e-shared-supabase`, evaluated **before** file classification — so docs/avatar-tool PRs (which never start Playwright and never touch Supabase) still **queued behind full main/PR runs**, wasting minutes for zero benefit (observed repeatedly: #108/#110 fast runs sat `pending` behind the forced-full main run from the previous merge). **(1) TWO JOBS:** workflow-level concurrency removed. A new **`classify`** job (no shared lock) checks out, paginates `pulls.listFiles` via `actions/github-script@v7`, runs the tracked classifier, and outputs `mode`, `concurrency_group`, base64-encoded `changed_files`, and `reason`. The required **`test`** job now `needs: classify`, `if: always()` (so the required check still reports even if classify fails), and takes **job-level** `concurrency: ${{ needs.classify.outputs.concurrency_group || 'e2e-shared-supabase' }}` with `cancel-in-progress: false`. **(2) LOCK ROUTING** (`concurrencyGroup(mode,runId)` in `.github/ci/classify-changes.mjs`, unit-tested): **full →** `e2e-shared-supabase`; **docs/avatar-tool →** isolated `ci-fast-<run_id>` (unique per run, never blocks on the shared lock). **(3) FAIL-CLOSED to full + shared:** non-PR/push-to-main, classifier/import/API error, oversized (>3000) file list, unknown/empty mode, missing/invalid run id, or missing job output all resolve to `mode=full` + `group=e2e-shared-supabase`. A belt-and-suspenders guard forces the shared group whenever `mode==full`, and the `test` job **re-normalises** mode (fail-closed to full) before any step condition — so a full suite can **never** run under a fast group. If a fast mode's changed-files list fails to decode, the check **fails clearly** rather than escalating to a lockless full run. **(4) full stays serialized** with `update-avatar-goldens.yml` through `e2e-shared-supabase` — that workflow is **unchanged** (its shared-group contract preserved). **(5) TEST COVERAGE UNCHANGED:** docs → classifier+concurrency unit tests + `git diff --check` + mojibake; avatar-tool → + `node --check`; full → the whole existing suite (npm ci, browsers, global setup, retries, timeout, secrets) — all identical. **(6) BOUNDARIES:** only `.github/workflows/playwright.yml`, `.github/ci/classify-changes.mjs`, `tests/unit/ci-classify-changes.test.mjs`, this doc (+ ROADMAP). No app runtime, HTML/CSS, asset, golden, database, RLS, Edge Function or existing-browser-test change; **`AVATAR_R2` stays `false`.** Unit: 37/37 (22 original classifier + 15 new concurrency). This PR edits `.github/workflows/**` → classifies **full** → runs the whole suite under the shared lock (proving workflow changes stay protected). The docs-only proof (fast run starts without waiting on the main lock) is a **separate follow-up PR**. Preserves D-032/D-033/D-042…D-066. (branch `ci/fast-modes-outside-supabase-lock`) (2026-07-24) **· Hardening (pre-merge review, 2026-07-24):** four fail-closed holes closed. **(a) Partial-output safety:** the `test` job lock no longer trusts the classify `concurrency_group` output alone — the job-level `concurrency` expression derives the group from **`needs.classify.result == 'success'` AND a validated `mode` of docs/avatar-tool**, computing `ci-fast-${{ github.run_id }}` directly from the trusted run id; classify failure/cancelled/skipped, missing/invalid/full mode or partial outputs all → `e2e-shared-supabase` (mirrored by the unit-tested pure `jobLockGroup(result,mode,runId)`; the output is kept for logs only). **(b) Fast-mode payload is mandatory:** the mode-normalise step now **fails clearly** (`core.setFailed`, no fast checks, no escalation to full while on the fast lock) when `changed_files_b64` is empty, undecodable, not an array, empty, contains a non-string/empty entry, or a path escapes the mode's safe set — via the pure `validateFastPayload(mode,files)`. **(c) `>= 3000` cap:** `pulls.listFiles` returns at most 3000, so a list AT the cap may be truncated — `fileCountForcesFull(n)` now trips at `>= MAX_CHANGED_FILES (3000)` → full/shared. **(d) Output-size guard:** `outputTooLarge(len)` forces full/shared and drops the payload when the base64 exceeds `MAX_OUTPUT_B64_LEN (512 KB)` (full does not need the list). Unit: **67/67** (37 prior + 30 new hardening). Workflow-only + tests + this doc; `AVATAR_R2` stays `false`. |
| **D-068** | **D-042 expression production complete → four owner-approved expression face layers PROMOTED to the tracked Avatar R2 asset structure (dormant, no runtime change). 2026-07-24.** After the D-042 deterministic Master-composite pipeline (donor = brows/nose/mouth only; frozen Master D-032 authoritative outside the write mask; Gate A/B/D + owner visual sign-off) produced the positive expression set, the owner accepted **proud v1, curious v1, focused v2, determined v2** and rejected **focused v1** (too stern/sour) and **determined v1** (too sly/aggressive, crooked smirk). **(1) DETERMINISTIC ALLOWLIST PROMOTION:** new tool `tools/avatar/promote-expression-layers.mjs` SHA-pins the exact four approved sources and hard-fails on unknown expression / wrong version / wrong source-SHA / wrong dims / missing alpha / wrong destination / the two rejected v1 SHAs / a changed Master / a changed `AVATAR_R2` — so no rejected or wrong candidate can be selected by directory order or timestamp. It encodes via the existing `encode-webp.mjs --half` (identical `-q 90 -alpha_q 100 -m 6 -metadata none -resize 512 0` contract as the six neutral layers, D-018), double-encodes for determinism, and verifies each output is 512×768 with alpha. **(2) FOUR TRACKED WEBP ASSETS** under `assets/avatar-r2/face/` (source→output SHA): `face-proud-v1.webp` (src `2B8D1453…` → `8BDED8BE…`, 3,336 B), `face-curious-v1.webp` (`C76F57DF…` → `CC1CA222…`, 3,848 B), `face-focused-v2.webp` (`C19BFF26…` → `D71F3FEA…`, 2,736 B), `face-determined-v2.webp` (`1F7569AC…` → `21F0665F…`, 3,150 B); total ≈12.8 KB (≪ ADR-163D budget). Not byte-identical to source (deterministic lossy q90 + ÷2), alpha near-perfectly preserved. Model provenance `gpt-image-2-2026-04-21`. **(3) MANIFEST (dormant):** `R2_MANIFEST` (`js/avatar-layers.js`) `version 3→4`; `face` gains `proud:1, curious:1, focused:2, determined:2`. **DORMANT by construction** — `faceSrcForR2` is called only with `"neutral"`, so the neutral stack is byte-for-byte unchanged and no expression asset is ever requested until the separately gated expression-wiring step. Unit `tests/unit/avatar-r2-expressions.test.mjs` (versions/paths/dormancy/no-v1-leak); full unit suite 143/143. **(4) NOT DONE / SEPARATE:** shared R2 raster artefacts (eyes, hair/forehead, arms, shoes) live in the shared base/eyes/hair stack — **not** the expression layers — and are a separate outstanding visual blocker, **not** fixed here and **not** an approval of broad R2 activation; small-size expression legibility (may need supplementary animation/aura/pose/icon/text) noted for the wiring decision. **(5) BOUNDARIES:** no render/engine/animation/eligibility/fallback change; **`AVATAR_R2` stays `false`**; Northstar Master byte-identical (`2CA10EF8…`); the six neutral layers + D-057/D-058 untouched; no API call, no image generation, no manual edit/feather/recolour/re-extraction; `curious` accepted with the owner note "reads slightly more surprised than curious at large size". Worksheet: `docs/167a-phase2-gate3-expression-asset-promotion.md`. Preserves D-032/D-033/D-042…D-067. (branch `avatar/promote-approved-expressions`) (2026-07-24) |
| **D-069** | **R2 expression wiring — the promoted expression layers now render on the R2 path via a face-layer swap (behind opt-in; `AVATAR_R2` stays `false`). Owner-approved plan (`167a-phase3-expression-wiring-plan.md`), 2026-07-25.** The `ExpressionEngine` emits exactly the five promoted layers; on C2 it cross-fades its own z0 overlay, on R2 it now drives the **existing decomposed z3 face layer** (`[data-c2-layer="face"]`, already tagged by the render — no render change needed). **(1) ENGINE R2-MODE:** `new ExpressionEngine(container, { r2: true })` borrows the face layer instead of creating an overlay and resolves src via `cdnUrl(faceSrcForR2(name))`; the state machine (state+event layers, priorities, hold/fade timings, reduced-motion instant-swap) is **unchanged**. Missing/failed expression asset → `neutral` (mandatory, already decoded) — aligns with the D-062 whole-stack-or-C2 contract. A `forceExpression(name)` test seam (no production caller) enables deterministic goldens (mirrors `BlinkEngine.forceFrame`). **(2) SURFACES:** enabled on all three where the engines run — quiz (`app.js`), `avatar.html`, `hub.html` (each `r2Active ? {r2:true} : C2`). **(3) BLINK/BREATHING UNAFFECTED:** the eye boxes are excluded from the expression write mask, so blink (z4 Option-A lids) and breathing (base) compose independently through every expression. **(4) CI-SAFE:** in CI `AVATAR_R2=false` + no opt-in → `r2Active` false → the R2 branch never runs in the default goldens; the self-served R2 goldens stay neutral (no state changes triggered on the static previews), so existing goldens are unaffected. C2 path byte-for-byte unchanged. **(5) TESTS:** `tests/unit/avatar-r2-expression-engine.test.mjs` (R2 targets the face layer, no overlay created, src swaps for all five, unknown→neutral, destroy restores neutral, C2 still owns its overlay); full unit suite **148/148**. **(6) FOLLOW-UP (not in this PR):** the per-surface **expression goldens** (self-served + fixture-intercepted, forced-frame per expression, win32 + linux baselines via the manual regen workflow) are the finalization step — the `forceExpression` seam is in place. Small-size supplementary cue remains a separate later track. **(7) BOUNDARIES:** `AVATAR_R2` stays `false` (no activation/flag-flip); no eligibility change; no shared-R2-artefact fix; Master untouched; assets/manifest unchanged (D-068 layers consumed as-is). Preserves D-032/D-033/D-042…D-068. (branch `feat/avatar-r2-expression-wiring`) (2026-07-25) |
| **D-070** | **R2 expression goldens — deterministic visual + DOM protection for the five expression face-layer swaps (finalises D-069). 2026-07-25.** `tests/avatar-r2-expression-frames.spec.ts` — SELF-SERVED (a local static server serves this branch's code, which carries the R2 mode + the gated `forceExpression` seam production lacks) + FIXTURE-INTERCEPTED (0 backend, no shared-student mutation). Determinism: reduced motion freezes breathing + suppresses auto-blink (eyes open), and `forceExpression()` pins a static face frame (instant swap). **(1) GOLDENS (chromium; win32 committed + linux via the manual regen workflow):** avatar.html all five (`r2-avatar-page-expr-{neutral,curious,focused,determined,proud}`), hub.html + quiz (`r2-hub-avatar-expr-{neutral,proud}`, `r2-quiz-avatar-expr-{neutral,proud}`). **(2) INVARIANTS (all browsers):** for each expression — renderPath `r2`, face src = the expected tracked WebP (`face-<expr>-v{1|2}.webp`), full R2 stack (≥4 markers), one base, no C2 `-c2.svg` mixing, no broken images, and **container/base/hair/face bboxes unchanged across expressions** (proves a pure src swap, not a layout change). Plus destroy → neutral face restored + gated handle cleared, and the handle is exposed **only** under `window.__AVATAR_TEST__` (production never exposes `window.__avatarExprEngine`). **(3) TEST HANDLE:** `js/avatar-expression-engine.js` registers `window.__avatarExprEngine` only when `__AVATAR_TEST__===true` (mirrors the blink handle); `destroy()` clears it. **(4) REGEN:** added the spec to `update-avatar-goldens.yml` (`--grep "golden:"`) for the Linux baselines. **(5) BOUNDARIES:** test-only + the gated handle; no render/asset/manifest change; `AVATAR_R2` stays `false`; C2 unchanged. Verified locally: win32 baselines generated + stable re-run (5/5). Preserves D-032/D-033/D-042…D-069. (branch `test/avatar-r2-expression-goldens`) (2026-07-25) |
| **D-071** | **Shared R2 raster-artefact render-scale audit CLOSED — owner accepts the D-061 residual for a controlled pilot; NO raster fix now. Read-only audit + docs only. 2026-07-26.** Before deciding any image correction on the shared R2 raster stack (base/arms/hair/shoes/eyes), the artefacts were measured and judged **at the sizes the app actually draws the avatar**, on the real render paths. **(1) METHOD (read-only):** a self-served + fixture-intercepted harness rendered the neutral stack (neutral · medium · tousled · brown · no cosmetics; expression forced neutral, blink forced open, reduced motion, `deviceScaleFactor 1`) through the real render code on `avatar.html`, `hub.html` and the quiz, in **both viewports (desktop 1440×900, mobile 390×844)**, under **R2 and C2 in identical identity/outfit/container conditions**, over the **real app background** and a **diagnostic dark** background. 100 %-view captures were the decision basis; zoom crops were **diagnostic only**. No backend mutation; no asset, runtime, manifest or flag change. Audit run read-only on `main` @ `34508cc`. **(2) ACTUAL RENDER SIZES (the decisive fact):** avatar **180×270** CSS-px (largest anywhere), hub **110×165** desktop / **100×150** mobile, quiz **52×78** — i.e. the shared artefacts are downscaled ≈2.8–9.8× from the 512-px asset (≈5.7–19.7× from the 1024-px Master) before any user sees them; mobile never renders larger than desktop. **(3) RESULTS — severity per area (desktop and mobile identical):** arms/hands `NOT_VISIBLE_AT_REAL_SCALE`; hair silhouette `NOT_VISIBLE_AT_REAL_SCALE`; shoes `NOT_VISIBLE_AT_REAL_SCALE`; forehead/hairline `NOT_VISIBLE_AT_REAL_SCALE`; eyes `NOT_VISIBLE_AT_REAL_SCALE`. The arm residual is a **very small anti-aliasing ramp visible only under heavy diagnostic magnification on an artificial dark background** — it exists diagnostically but is **not user-visible at the current render sizes**. **(4) R2 vs C2:** R2 and C2 use comparable outer render boxes; **R2 adds no new user-visible edge artefact** over C2 and is a **clear visual upgrade** from the existing C2 placeholder. The automatic per-pixel artefact counts are support only and **must not be read as pixel-identity between R2 and C2**. **(5) BINDING OWNER DECISION — `OWNER_ACCEPTED_FOR_PILOT_WITH_DOCUMENTED_RASTER_DEBT` (decision D):** no alpha-oprydning, no source re-cut, no new image production; the **D-061 residual is accepted for a controlled pilot**; the shared raster artefacts are **no longer a blocker at the existing render sizes**. This is **not** broad R2 activation — **`AVATAR_R2` stays `false`.** An alpha operation or source re-cut would treat an invisible problem while risking silhouette/identity, so it is declined. **(6) RE-AUDIT TRIGGERS (reopen the raster track on any of):** a new avatar surface wider than ≈180 CSS-px; a fullscreen/hero avatar; a materially higher actual display scale; a new dark theme where edges become visible at 100 %; any change to base/hair/shoe assets; owner or pilot users observing visible fringe in normal use; a device-pixel/render-strategy change that makes asset edges more prominent. A re-audit must again use real app render size as the primary basis. **(7) REMAINING SEPARATE R2 TRACKS (none activated here):** controlled R2 pilot · broad R2 activation · optional supplementary cue at small avatar sizes · pilot findings. **Small-size expression legibility is a distinct concern and is NOT the raster-fringe problem.** **(8) HISTORY PRESERVED:** D-059/D-060/D-061 remain valid — the remediation work was necessary and is **not** rewritten; the artefacts are **not** claimed to be physically removed, only shown to be non-user-visible at current render sizes. **(9) BOUNDARIES:** docs-only (this register + `docs/ROADMAP.md` + `docs/AVATAR_SYSTEM.md`); the temporary untracked audit spec was deleted; the gitignored audit scratch (`tools/avatar/build/phase2/gate3-d057/artefact-inspect/render-scale-audit-2026-07-26/`) is preserved and not staged; no asset/runtime/manifest/golden/database/RLS/Edge-Function change; Master byte-identical (`2CA10EF8…`); `AVATAR_R2` stays `false`. Preserves D-032/D-033/D-042…D-070. (branch `docs/avatar-r2-raster-debt-pilot-decision`) (2026-07-26) |
| **D-072** | **Controlled R2 pilot — status corrected + operational onboarding protocol finalized. Docs-only (2026-07-26).** A prior log entry recorded the dedicated test-student as `ONBOARDED` after a live production check, but that check ran in an **ephemeral automated (Playwright) browser context** with **no persistent browser profile** and **no evidence the `localStorage` opt-in survives a browser restart**. **(1) WHAT WAS PROVEN vs NOT:** PR #119 proved a genuine **live R2 render on production** — on avatar, hub and quiz `renderPath=r2` (no C2 fallback), neutral-medium base, all six layers, no broken images, no C2 mixing, neutral-medium eligibility read from the live profile; no user data changed, no quiz answer submitted. It did **not** prove **persistent onboarding** or **real pilot participation**. The correct classification is therefore **`LIVE_VERIFIED_IN_EPHEMERAL_TEST_BROWSER`**, not `ONBOARDED` (the correction does **not** assert the key was deleted — only that persistence was not proven). **(2) DOC CORRECTION:** `docs/167a-phase1-pilot-rollout.md` §15 row 1 changed `ONBOARDED` → `LIVE_VERIFIED_IN_EPHEMERAL_TEST_BROWSER`; any UID/account identifier kept out of the docs. **(3) OVERALL PILOT STATUS = `AUTHORIZED_BUT_NOT_STARTED`** — mechanism ready, R2 live-verified technically, raster debt accepted (D-071), but **no user is yet a persistent pilot participant**; status lines + roadmap no longer imply the pilot is running. **(4) BINDING PERSISTENT-BROWSER GATE (§8):** a user is `ONBOARDED` only after a 13-step gate performed in the **actual persistent pilot browser** — including a full **close → reopen → opt-in still present → `renderPath=r2` → no mixed stack** cycle + an opt-out demo; before that cycle a user is at most `PERSISTENT_ONBOARDING_PENDING`; an automated Playwright verification does **not** satisfy the gate. **(5) WAVE 1:** target **3**, maximum **5** without a new owner decision; one primary browser/device per user; one at a time; internal/closely-supervised; no whole class / public rollout / automatic cohort; the test-student may be #1 only after the gate. **(6) DURATION & EXPOSURE:** 7 calendar days, ≥3 real sessions/user, ≥1 observed session each on quiz/hub/avatar, ≥1 desktop (mobile when relevant); 7 idle days do not count. **(7) SUCCESS / FEEDBACK / SEVERITY:** success requires active observation or concrete feedback (absence of error reports ≠ success); data-minimal feedback-log template added; severity vocabulary `INFO`/`MINOR`/`MAJOR`/`BLOCKING`; no name/email/UID/token/localStorage in the log. **(8) ABORT + FINAL CLASSIFICATIONS:** immediate-pause abort criteria + `PILOT_PASS` / `PILOT_PASS_WITH_DEBT` / `PILOT_PAUSED` / `PILOT_FAILED`; **no classification may auto-set `AVATAR_R2=true`.** **(9) BOUNDARIES:** docs-only (`167a-phase1-pilot-rollout.md`, this register, `ROADMAP.md`); no runtime/asset/manifest/golden/test/workflow change; no user activation; no browser automation; Master byte-identical (`2CA10EF8…`); **`AVATAR_R2` stays `false`.** **D-071 is unchanged** (raster-debt acceptance) and its re-audit triggers are preserved as pilot guardrails (§14). Preserves D-032/D-033/D-042…D-071. (branch `docs/avatar-r2-pilot-status-correction-d072`) (2026-07-26) |
| **D-073** | **Manual persistent-browser onboarding kit created for the controlled R2 pilot. Docs-only (2026-07-26).** The next binding pilot gate (D-072 §8) is a **manual** onboarding in the actual persistent browser profile — not automatable and not satisfied by a Playwright check. This adds the copy-ready procedure the owner needs. **(1) NEW DOC:** `docs/167a-persistent-browser-onboarding-kit.md` (Danish, `READY_FOR_MANUAL_EXECUTION`) — followable without reading the codebase; allowed end-statuses `ONBOARDED` / `BLOCKED` / `OPTED_OUT`. **(2) RUNTIME COMMANDS VERIFIED & DOCUMENTED (no new mechanism):** the `localStorage` key `avatar_r2`, the existing **enable** (`setItem("avatar_r2","1")` + reload) and **disable** (`removeItem("avatar_r2")` + reload) commands (incl. the §4 bookmarklets verbatim), a read-only **status** check (`getItem("avatar_r2")`), a read-only **render-path** check (`dataset.avatarRenderPath` → `"r2"`), and a read-only **stack-integrity** check (asserts `[data-c2-layer="base"]` present, no `-c2.svg` mixing, no broken images, no state change) — all verified against `js/avatar-render-c2.js` / `isAvatarR2()`. **(3) STRUCTURE:** Fase A (first activation) is **separated from** the persistence-gate; the persistence-gate is **separated from** the opt-out demo. **`ONBOARDED` requires a full browser close + reopen of the same profile, opt-in surviving the restart, `renderPath=r2` re-confirmed, no mixed stack**, correct identity, and a demonstrated opt-out; before that a user is at most `PERSISTENT_ONBOARDING_PENDING`. **(4) SAFETY/PRIVACY:** data-minimal log template + severity `INFO`/`MINOR`/`MAJOR`/`BLOCKING`; an error table (BLOCKED/BLOCKING/MAJOR/INFO handling) with **no runtime workaround** anywhere; no name/email/UID/token/localStorage in the log. **(5) NOTHING EXECUTED:** no user was onboarded, no browser opened, no `localStorage` changed, no login, no API/Supabase call. **(6) BOUNDARIES:** docs-only (`167a-persistent-browser-onboarding-kit.md`, `167a-phase1-pilot-rollout.md` additive refs, this register, `ROADMAP.md`); no runtime/asset/manifest/test/golden/workflow change; `.env` untouched; Master byte-identical (`2CA10EF8…`); **`AVATAR_R2` stays `false`; pilot status stays `AUTHORIZED_BUT_NOT_STARTED`; test-student stays `LIVE_VERIFIED_IN_EPHEMERAL_TEST_BROWSER`.** D-071 and D-072 are **not** rewritten. Preserves D-032/D-033/D-042…D-072. (branch `docs/avatar-r2-persistent-onboarding-kit-d073`) (2026-07-26) |
| **D-074** | **Pilot observability (C-track) AUDITED + DESIGNED — no implementation. Docs-only (2026-07-26).** Read-only audit of the R2 render control flow to find the least-risky way to observe whether an opt-in browser renders R2, cleanly falls back to C2, or hits a render failure — without user data and without affecting rendering. **(1) CURRENT STATE (verified vs code):** all three surfaces mount through the single shared `mountC2Avatar` (quiz `app.js:502`, avatar `avatar.html:979`, hub `hub.html:1580`), which **already returns** `"r2"`/`"c2"`/`"aborted"` and stamps `dataset.avatarRenderPath` — so a **single central emission point** with **no API change** is possible; the mandatory-asset atomic gate (`:179-185`) already distinguishes the asset-failure fallback; `render_failed` is **not** currently produced (no overall try/catch) and would need a guarded wrapper in the impl PR; blink/breathing/expression-swap do **not** call `mountC2Avatar` (no double-events). **(2) RECOMMENDED MODEL — `CONSOLE_ONLY_PILOT_OBSERVABILITY`:** structured `console.info` (`render_failed` via `console.warn`), **no backend, no beacon, no database, no persistence, no identifier**, emitted **only** when `localStorage.avatar_r2 === "1"` (normal C2 users stay silent). **(3) EVENT SCHEMA (v1, frozen):** `{ event:"avatar_r2_render", version:1, surface:"avatar"|"hub"|"quiz", result:"r2"|"c2_fallback"|"render_failed", reason }`; console prefix `[avatar-r2-observability]`; no error text, no explicit timestamp. **(4) REASON-CODES (only code-supported):** `required_asset_failed`, `identity_ineligible`, `forced_c2`, `render_exception` (with the wrapper), else `unknown`; missing-vs-decode split deferred (not safely distinguishable). **(5) SAFETY:** fail-soft (emission wrapped, never awaited, cannot break rendering); dedup via a module-local `WeakSet` of roots (≤1 result/root/load, resets on reload); **privacy boundary** forbidding all PII (name/email/UID/session/token/quiz/progression/cosmetics/localStorage/raw errors/stack traces). **(6) TEST STRATEGY (later PR):** unit (one event per outcome, none without opt-in, throwing helper doesn't affect render, no PII, dedup, blink/breathing/expr emit nothing, C2 default unchanged) + one self-served, fixture-intercepted Playwright spec via `page.on("console")` (no backend, no shared-student mutation). **(7) NON-GOALS:** no dashboard/analytics/Supabase table/Edge Function/beacon/retention/tracking/fingerprint/flag-service/allowlist/UI-toggle/auto-classification. **(8) BOUNDARIES:** docs-only (`167a-r2-pilot-observability-design.md` new, `167a-phase1-pilot-rollout.md` additive ref, this register, `ROADMAP.md`); **no runtime/asset/manifest/test/golden/workflow change; no console log added; `.env` untouched; Master byte-identical (`2CA10EF8…`); `AVATAR_R2` stays `false`; pilot status stays `AUTHORIZED_BUT_NOT_STARTED`; test-student stays `LIVE_VERIFIED_IN_EPHEMERAL_TEST_BROWSER`.** **Owner decision required** before any runtime PR (`DESIGN_READY_AWAITING_OWNER_DECISION`). D-071/D-072/D-073 not rewritten. Preserves D-032/D-033/D-042…D-073. (branch `docs/avatar-r2-pilot-observability-d074`) (2026-07-26) **· Owner decision CONFIRMED (2026-07-26): `CONSOLE_ONLY_PILOT_OBSERVABILITY`** — design-doc status now `OWNER_DECISION_CONFIRMED`; a later runtime PR may implement exactly §6–§18 (console-only, pilot-gated, fail-soft, WeakSet dedup, **no backend/database/identifier/persistence**). Confirmation authorises the **design, not activation**: no user onboarded, observability stays advisory (never gates R2 rendering), `AVATAR_R2` stays `false`, pilot status stays `AUTHORIZED_BUT_NOT_STARTED`. Docs-only; no runtime/asset/test/golden change. (branch `docs/avatar-r2-observability-owner-decision`) |
| **D-075** | **Allowlist enablement (B-track) AUDITED + DESIGNED — recommend DEFER. Docs-only (2026-07-26).** Read-only audit of how to enable R2 for specific pilot accounts centrally, without shipping student identifiers to the public client. **(1) CURRENT STATE (verified vs code):** the activation gate has **no uid** — `isAvatarR2()` (`avatar-layers.js:314`) is a pure per-browser check (`AVATAR_R2 || localStorage.avatar_r2==="1"`); `isAvatarR2ActiveFor(identity)` and `mountC2Avatar` see only the avatar **identity token** (no uid/email/account-id). An id-keyed allowlist would require **plumbing the uid into the gate (a data-flow change)** plus an id→eligibility store. **(2) PRIVACY/GDPR CRUX:** the app serves **children**; the frontend bundle is public, so any hardcoded/committed/build-injected UID list **ships children's identifiers to every visitor** — unacceptable and contrary to the D-072/D-073/D-074 no-PII discipline. **(3) OPTIONS:** A client committed UID list — **rejected**; B build/env-injected UID list — still ships UIDs, not recommended; C opaque non-PII enable token — ergonomic wrapper over the existing opt-in, no server, low added value; D **server-side eligibility flag** (RLS-protected `profiles` boolean, client reads only its own) — the only *real* allowlist, but a backend + data-flow change; E **defer** — Wave 1 (3–5) is already enabled per-browser via the D-073 kit with zero PII/code change. **(4) RECOMMENDED DECISION `NO_ALLOWLIST_FOR_PILOT`:** build no allowlist now; keep per-browser opt-in; if scale ever requires central enablement use **option D (server-side flag)**, never a client UID list. **(5) NON-GOALS:** no client UID list, no percentage/auto-cohort, no broad activation, no UI toggle, no telemetry, no change to the manifest neutral-medium gate. **(6) BOUNDARIES:** docs-only (`167a-r2-pilot-allowlist-design.md` new, `167a-phase1-pilot-rollout.md` additive ref, this register, `ROADMAP.md`); no runtime/asset/manifest/test/golden/workflow change; no migration; `.env` untouched; **no student identifier committed anywhere**; Master byte-identical (`2CA10EF8…`); **`AVATAR_R2` stays `false`; pilot status stays `AUTHORIZED_BUT_NOT_STARTED`; test-student stays `LIVE_VERIFIED_IN_EPHEMERAL_TEST_BROWSER`.** **Owner decision required** (`DESIGN_READY_AWAITING_OWNER_DECISION`). D-071…D-074 not rewritten. Preserves D-032/D-033/D-042…D-074. (branch `docs/avatar-r2-allowlist-design-d075`) (2026-07-26) **· Owner decision CONFIRMED (2026-07-27): `NO_ALLOWLIST_FOR_PILOT`** — design-doc status now `OWNER_DECISION_CONFIRMED`; no allowlist is built, Wave 1 stays on the per-browser opt-in (D-073); client-side UID lists (A/B) rejected on GDPR grounds; the future path if scale ever demands it is a server-side eligibility flag (option D, its own audited runtime+migration PR). Changes nothing at runtime: no code, no migration, no data-flow change; `AVATAR_R2` stays `false`; pilot status `AUTHORIZED_BUT_NOT_STARTED`. Docs-only. (branch `docs/avatar-r2-allowlist-owner-decision`) |
| **D-076** | **Console-only pilot observability IMPLEMENTED (runtime) — the owner-confirmed D-074 design. First runtime PR of the pilot arc (2026-07-27).** **(1) NEW HELPER `js/avatar-r2-observability.js`:** `emitR2RenderObservability({surface,result,reason,root})` — advisory, fail-soft (can never throw), pilot-gated on the EXPLICIT browser opt-in `localStorage.avatar_r2==="1"` (read at emission; `AVATAR_R2=true` alone does not trigger it), WeakSet-deduped (≤1 result per root per page load), frozen payload `{event:"avatar_r2_render",version:1,surface,result,reason}`, prefix `[avatar-r2-observability]`; `render_failed`→`console.warn`, else `console.info`, never `console.error`; **no backend/network/database/persistence/PII** (no uid/email/token/session/error/stack/timestamp). Invalid surface/result → silent; invalid reason → `unknown`. **(2) CENTRAL EMISSION in `js/avatar-render-c2.js`:** one call just before the final `return path` in `mountC2Avatar`, only when path is `"r2"`/`"c2"`, root present, mount current — never on `"aborted"`/`!rootEl`. **RENDER_FAILED via a read-only-audit-approved overall try/catch** that emits `render_failed`/`render_exception` then **RE-THROWS the identical error** (audit proved: returns, Promise resolve/reject, stack, fallback and caller semantics are unchanged; the mandatory-asset catch stays inside and still returns `"c2"` → classified `required_asset_failed`, never render_failed). **(3) REASON-MAPPING (only code-distinguishable):** r2→`unknown`; mandatory-asset-catch→`required_asset_failed`; `forceC2`→`forced_c2`; `composeR2Layers` null (opt-in, not forced)→`identity_ineligible`; unhandled exception→`render_exception`. **(4) EXPLICIT SURFACE** added as an optional `mountC2Avatar` option and passed from the three production call-sites (`app.js` "quiz", `avatar.html` "avatar", `hub.html` "hub"); no fragile layerClass derivation; `shop.html` passes no surface → previews stay silent. **(5) TESTS:** `tests/unit/avatar-r2-observability.test.mjs` (19 cases: gating, validation, schema/PII whitelist, dedup, not-opted-in-does-not-block-later, console-throw/localStorage-throw fail-soft, never console.error) + `tests/avatar-r2-observability.spec.ts` (self-served + fixture-intercepted, `page.on("console")`, fictitious fixture token: one r2 event per avatar/hub/quiz, mandatory-asset-404→c2_fallback/required_asset_failed, no-opt-in→silent, rerender-dedup, injected-exception→render_failed/render_exception). Local: unit 19/19, spec 7/7, existing R2 goldens 5/5 unchanged. **(6) BOUNDARIES:** `AVATAR_R2` stays `false`; `isAvatarR2()` opt-in contract unchanged; **C2-default functionally/visually/DOM unchanged**; no asset/`R2_MANIFEST`/golden/migration/workflow change; observability is advisory and never gates rendering; pilot status stays `AUTHORIZED_BUT_NOT_STARTED`; test-student stays `LIVE_VERIFIED_IN_EPHEMERAL_TEST_BROWSER`; no user onboarded. Implements the D-074 design (status there stays `OWNER_DECISION_CONFIRMED`, additive note only); D-071…D-075 not rewritten. Preserves D-032/D-033/D-042…D-075. (branch `feat/avatar-r2-observability-d076`) (2026-07-27) |

## Completed Sections
155A–155I · 156A–156C · [prod-apply 155E/155F] · 157 · 158A–158C · [ROOT sync] ·
159A–159G · [ROOT sync] · 160 · 161A · 161B · 161B.5 (docs baseline committed) ·
161C–161E · 162A–162B (North Star spec + prompt package) ·
163A (Hybrid Raster arch) · 163B (Eye System ADR) · 163C (eye docs) · 163D (pipeline ADR) ·
163F (decomposition & raster asset spec) · 163G (MVP scope decisions D-020…D-027) ·
163H (raster asset spec documentation: ADR-163F + state/vision update + consistency check) ·
164A (North Star Master v1.0 decomposition spec — **COMPLETE**; decomposition locks D-028…D-031) ·
164B-prep (D-032 source-of-truth split + reference assets) · 164B.1 (asset production plan) ·
164B.2 (base reconstruction spec) · 164B.3 (base review gate + worksheet) · 164B.4 (base prototype input brief) ·
164C (Base Production Method Pivot — **D-033**: manual paint-over base, AI rejected) ·
164D (Scalable Shop Item Pipeline & Slot Template Architecture — **D-034**: slot-constrained
overlays, AI for items only; spec `docs/164d-shop-pipeline.md`) ·
164E (Slot/Z reconciliation — **D-035**: `C2_LAYER_Z` canonical, legacy `SLOT_Z` frozen;
new-slot z-values **reserved (8/15/25/100), activation deferred**) ·
164F (Shop product taxonomy — **D-036**: accessory-first MVP = aura/back/headwear/face(masks)/
eyes(glasses); torso conditional; body/shoes/bottom/hands/front_fx deferred; hair = identity only) ·
164G (MVP shop QA gates & mask spec — **D-037**: gate framework + per-slot mask rules locked;
mask assets are Tier-1 work pending base/rig from 164B) ·
164H (Tier-1 base rig & mask authoring plan — **D-038**: locks the 6-step Tier-1 sequence
+ formats/gates/order; base rig is the blocking first step; spec `docs/164h-tier1-base-rig-mask-authoring-plan.md`) ·
164I (Base rig production execution method — **D-039**: Option B (outsource to illustrator)
primary, A fallback, C scaffold-only; 164B.3 PASS gate; spec `docs/164i-base-rig-production-execution-plan.md`) ·
164J (Automation-first avatar production — **D-040**: Master = Tier-0 base; MVP = accessory
overlays only; neutral base/illustrator method re-scoped to a deferred upgrade; D-038/D-039 re-scoped) ·
164K (Automated anchor + MVP mask extraction plan — **D-041**: deterministic non-AI derivation
from Master; method/schema/gates locked; spec `docs/164k-anchor-mask-extraction-plan.md`).

## Current Production State (avatar MVP path — D-040)
- **Tier-0 base = `Northstar Master.png`** (fixed default avatar; no reconstruction needed for MVP).
- **MVP shop = accessory overlays only** (aura, back, headwear, face/masks, eyes/glasses).
- **Blocking next step = 164L** (the deterministic non-AI extraction tooling; 164K spec locked by D-041).
- **Deferred upgrades (not MVP blockers):** neutral reconstructed base (164B/164I/D-039),
  per-user skin tone / hairstyle / hair-color, torso/bottom/shoes clothing slots.

### 167A raster migration — execution path LOCKED (2026-07-01)
- **Step 1 scaffold shipped** (commit `8e089af`): `assets/avatar-r2/` + empty `R2_MANIFEST` + inert
  raster resolvers in `js/avatar-layers.js` (`AVATAR_R2=false`; render on C2/SVG). Additive; no
  render/engine/z/identity change.
- **Path LOCKED = D-040 Phase-1 "Master-as-is" FIRST**, then 163F Phase-2 decomposition later
  (**Phase-2 not started**). Step-3 wiring plan: [`167a-step3-render-wiring-plan.md`](./167a-step3-render-wiring-plan.md).
- **Step 3a DONE (2026-07-01):** the Phase-1 baked base is produced + wired. A deterministic extractor
  (`tools/avatar/extract-master-base.mjs`) alpha-cuts + downscales the Master → a **temporary transparent
  PNG preview** `assets/avatar-r2/base/body-neutral-medium-v1.png`, registered in `R2_MANIFEST`
  (`base:{ "neutral-medium":{ v:1, ext:"png" } }`, `version:1`) and consumed by `mountC2Avatar` **only
  when `AVATAR_R2` is true (default false → untouched C2/SVG render)**. Engines/cosmetics/identity
  unchanged; C2 fallback intact. Validated (node --check, unit, smoke).
- **PNG is temporary; WebP is the production target (D-013)** — no image dependency was added (no
  sharp/cwebp). **WebP swap DEFERRED (2026-07-01):** no encoder is available in the environment
  (cwebp/ffmpeg/ImageMagick/sharp all absent) and adding a dep / hand-rolling an encoder was declined
  to avoid new deps. The **PNG (~244 KB, within the <350 KB budget) is the current Phase-1 runtime
  asset**; WebP swap (`{ v:2, ext:"webp" }`) waits until a proper encoder is available.
  > **UPDATE 2026-07-02:** an encoder is now available (vendored libwebp `cwebp.exe`, gate 4 — see
  > the Phase-2 gate list below). The Phase-1 WebP swap is therefore **unblocked**, but remains a
  > separate, deliberate action (encode `body-neutral-medium-v2.webp`, register `{ v:2, ext:"webp" }`);
  > not done here.
- **Phase-1 engine guard DONE (2026-07-01):** when the raster base is active
  (`isAvatarR2ActiveFor(identity)`), expression + blink overlays are **skipped** at the mount sites
  (app.js, avatar.html, hub.html) so they never render over the baked face; **presence/breathing stays
  on**. Engine logic unchanged (caller-side skip only, an already-supported "engine absent" state);
  C2/SVG path untouched. Verified by DOM probe (raster → 0 expr overlays, no blink layer, breathing
  active) + smoke.
- **Phase-1 cosmetic slot-gate DONE (2026-07-01):** on the raster base, `composeR2Layers` renders only
  `R2_PHASE1_SAFE_SLOTS = ["aura","back"]` (behind-figure, anchor-independent). Head/face/eye
  (headwear/face/eyes) + clothing (torso/body/neck) are gated — QA showed they float on the legacy
  anchors / clash with the baked outfit. Raster path only; C2/SVG renders all slots unchanged. No
  z-model/anchor/mask/shop/ownership/identity change. Cosmetic re-anchoring = Phase-2.
- **Phase-1 visual sign-off = PASS (2026-07-01)** — see
  [`167a-phase1-visual-signoff-checklist.md`](./167a-phase1-visual-signoff-checklist.md).
- **Phase-1 pilot opt-in LIVE (2026-07-01, commit `78a8e6d`).** `isAvatarR2()` now honours a
  per-browser override `localStorage.avatar_r2 === "1"` (mirrors `AVATAR_V2`). **`AVATAR_R2` remains
  `false` by default** → non-pilot users still render the untouched **C2/SVG** avatar; only browsers
  that set the key see the raster. **No DB cohorting, no percentage rollout, no global enable.**
  - **Pilot selection:** neutral-medium identity + low/no cosmetics (only aura/back render on the
    baked base; head/face/eye/clothing are slot-gated). Criteria + onboarding:
    [`167a-phase1-pilot-rollout.md`](./167a-phase1-pilot-rollout.md).
  - **Opt-out:** `localStorage.removeItem("avatar_r2")` (reload) → back to C2 instantly.
  - **Still deferred:** WebP swap (no encoder; PNG is the current runtime asset). **Phase-2 163F
    decomposition NOT started.**
- **Remaining (deferred): Phase-2 163F decomposition** (steps 3b/3c — living face/eyes/blink, hair
  raster + eye-box; cosmetic anchor revision) — **not started**; per-layer WebP art required.
- **Phase-2 PLANNING DONE (2026-07-01, PR #1 `5e2e80b`) — implementation NOT started.** Two docs
  merged: the decomposition audit + implementation plan
  [`167a-phase2-decomposition-plan.md`](./167a-phase2-decomposition-plan.md) (target stack, generated-
  vs-human-art split, revised **raster-only** eye-box, blink/expression reactivation, staged cosmetic
  un-gating, manifest/versioning, gates, rollback) and the P2-0 asset production brief
  [`167a-phase2-asset-brief.md`](./167a-phase2-asset-brief.md) (exact per-layer cut-list, dims/z/bg/
  producer/acceptance, North Star eye-box numbers, WebP + sign-off gates). Docs only — no runtime/
  manifest/flag change.
- **P2-0 cut-guide extractor SHIPPED (2026-07-01, PR #2 `bab6a8a`) — review artifacts only.**
  `tools/avatar/extract-phase2-cut-guides.mjs` (deterministic, non-AI, zero-dep; same PNG codec +
  sha256 guard + coordinate conventions as `extract-master-base.mjs`) reads the frozen Master (read-
  only) + the 164L anchor template and emits, to the **gitignored** `tools/avatar/build/phase2/`:
  `cut-guides-overlay-v1.png` (anchor regions + eye opening/iris/pupil crosshairs on the Master),
  `crop-{face,eye-left,eye-right,headwear,back}-region-v1.png` (per-zone crops), and
  `cut-guides-v1.report.json` (regions + eye centres in master / served ÷2 / engine-160 ÷6.4). It is
  the painter's guide for the human-authored Phase-2 layers — **NOT runtime assets, NOT geometry-
  altering; no `assets/avatar-r2/` write, no `R2_MANIFEST` change, `AVATAR_R2` untouched.** Verified:
  eye-opening centres L (66.7, 60.3) / R (90.6, 60.3) in 160-space (≈13 units below the legacy `cy47`
  blink box); output PNGs valid; visual QA of the overlay = regions land correctly on the figure.
- **P2-0 cut-guide review worksheet RUN + PASS (2026-07-01, PR #6 `7fa7560`; countersign PR #7
  `2159d3e`).** [`167a-phase2-cut-guides-review-worksheet.md`](./167a-phase2-cut-guides-review-worksheet.md)
  reviewed the overlay + crops (all 10 checklist rows PASS) and approved the **Phase-2 raster
  eye-box**. **Owner countersign = APPROVED 2026-07-01.** → **Plan §13 GATE 1 (Phase-2-scoped
  anchor/eye-box sign-off) is SATISFIED.** Approval is **raster-path only** — the eye-box drives the
  raster blink/eye rig; the **legacy C2 anchors (`cx68/92 cy47`, `js/avatar-blink-engine.js`) stay
  frozen** (two eye-box sets coexist, selected by the render branch). Docs only; no runtime/manifest/
  flag change.
- **Phase-2 implementation gates — GATES 1 + 4 SATISFIED; GATE 2 REOPENED / UNDER RECOVERY (2026-07-05);
  GATES 3 (PAUSED), 5 OPEN.**
  (1) ✅ Phase-2-scoped anchor/eye-box sign-off — **SATISFIED** (owner-countersigned, PR #7 `2159d3e`).
  (Re-check registration — offset (+25,+285) — once the base-layer source is aligned.)
  (2) 🔴 **v2 base 164B.3 — REOPENED / UNDER RECOVERY (2026-07-05).** The **iter7** base is
  **VISUALLY INVALIDATED** (skin-colored **bust/chest-plate** artifact = structural anatomy failure);
  the whole iter4→iter7 carve line is invalidated and the **owner-countersigned iter7 164B.3 CONDITIONAL
  PASS is WITHDRAWN/SUPERSEDED** (see [`164b3-iter7-base-review.md`](./164b3-iter7-base-review.md), now
  marked invalidated). **Registered base-layer source adopted (D-043, REVISED 2026-07-06):**
  `recovery-base-v1-blankface.png` (`source-master(3).png`, preserved at `assets/avatar/reference/`) —
  fixes the anatomy (correct bald scalp, ears, head→neck→collar, no bust-plate, blank face). It is a
  **candidate base-layer source registered to the frozen Master, NOT a new Master/datum** (Master remains
  the canonical identity/style/coordinate datum; **D-032 preserved, not superseded**). Verified
  falsification-tested: **same figure, same scale**, registers to the Master by a deterministic
  translation **(+25 x, +285 y)** (body IoU ≈ 0.9921; ≈ 84.3 % pixels identical). **NOT passed yet:** it
  needs **registration by the (+25,+285) offset** (anchors/eye-box/masks carry over; no re-datum),
  **outfit neutralization** (still green sweater+star/cargo/green shoes → plain grey, D-029/D-022) — with
  the cropped lower legs/feet **folded into that same masked pass** (Master = authoritative body/feet
  source; **not a separate pre-step**, decision 2026-07-06) — and a **fresh 164B.3 review + composited
  sign-off**.
  **NOT promoted; not a runtime asset; no `assets/avatar-r2/` write, no `R2_MANIFEST` change, `AVATAR_R2`
  `false`.** Decision: [`167a-phase2-base-recovery-decision.md`](./167a-phase2-base-recovery-decision.md).
  **Gate 2A DEFINED / PLANNED (2026-07-06):** a narrow, deterministic, review-first phase — registration of
  recovery-base into the Master frame (translate **(−25, −285)**), feet-completion audit, review-only
  composites, and a deterministic validation report; **excludes** outfit/face/eyes/eyelid/hair/runtime/
  promotion. **Gate 2A alone does NOT satisfy Gate 2.** Plan:
  [`167a-phase2-gate2a-registration-plan.md`](./167a-phase2-gate2a-registration-plan.md).
  **Gate 2A EXECUTED — PASS / owner-review-ready (2026-07-06):** registration validated (body IoU ≈ 0.9949;
  offset (−25,−285); Master-pixel preservation ≈ 83.9 %; missing feet region y1251–1508 ≈ 59,801 px).
  Review: [`167a-phase2-gate2a-registration-review.md`](./167a-phase2-gate2a-registration-review.md).
  **Feet/lower-leg completion DECISION (2026-07-06):** deferred into the neutral-outfit/base-assembly step
  (Option B, guided by Option D = Master body/feet + recovery head); **no completed base produced.**
  Decision: [`167a-phase2-feet-completion-decision.md`](./167a-phase2-feet-completion-decision.md).
  **Neutral-outfit / base-assembly PLAN DEFINED (2026-07-07):** strategy B (Master body/lower-legs/feet +
  recovery head/bald-scalp/blank-face → one masked neutral-outfit reconstruction pass); neutral target =
  light-grey short-sleeve tee / charcoal straight trousers / light-grey low sneakers; **short sleeves ⇒
  underarm reconstruction = highest-risk sub-area (own sub-gate).** Docs-only plan; next = deterministic
  base-assembly/mask tooling (review-first). Plan:
  [`167a-phase2-neutral-outfit-base-assembly-plan.md`](./167a-phase2-neutral-outfit-base-assembly-plan.md).
  **Gate 2A does NOT satisfy Gate 2 — Gate 2 stays REOPENED / UNDER RECOVERY.**
  **Base-assembly tooling BUILT + REVIEWED (2026-07-07):** `tools/avatar/build-base-assembly-masks.mjs`
  (deterministic Strategy-B assembly preview + approximate review-only mask proposals; head contribution
  localized; body-below-seam IoU 1.0000; artifacts gitignored) — tooling review **PASS /
  owner-review-ready**: [`167a-phase2-base-assembly-masks-review.md`](./167a-phase2-base-assembly-masks-review.md).
  **Still NOT a Gate-2 pass; no neutral outfit executed; no completed base; next = AI-assisted masked
  neutral-outfit candidate (after owner accepts), incl. the underarm sub-gate.**
  **✅ OWNER ACCEPTED / COUNTERSIGNED (2026-07-07):** the **Gate 2A registration review** and the
  **base-assembly tooling review** are both owner-accepted
  ([`167a-phase2-gate2a-registration-review.md`](./167a-phase2-gate2a-registration-review.md) ·
  [`167a-phase2-base-assembly-masks-review.md`](./167a-phase2-base-assembly-masks-review.md)).
  **Next step = AI-assisted masked neutral-outfit candidate planning/execution (D-042), incl. the underarm
  sub-gate. Gate 2 is still NOT satisfied (REOPENED / UNDER RECOVERY); Gate 3 still PAUSED; `AVATAR_R2`
  `false`.**
  **Mask-refinement tooling BUILT + REVIEWED + ✅ OWNER ACCEPTED / COUNTERSIGNED (2026-07-08):**
  `tools/avatar/build-neutral-outfit-mask-refinement.mjs` (deterministic, non-AI) refines the approximate
  base-assembly proposals into **edit-ready** masks + an explicit **protect mask** (identity-lock, outfit-edit
  ∩ head = 0 px asserted) + a per-region **prompt pack** — all gitignored/review-only. Tooling review **PASS /
  owner-review-ready** and now **owner-accepted / countersigned**:
  [`167a-phase2-mask-refinement-review.md`](./167a-phase2-mask-refinement-review.md). **Masks are NOT final
  production masks; no AI generation happened; no neutral outfit executed; no completed/final base exists.**
  **Next step = actual AI-assisted masked neutral-outfit candidate EXECUTION (D-042), incl. the underarm
  sub-gate → then fresh 164B.3 + composed Gate-5 sign-off. Gate 2 still NOT satisfied (REOPENED / UNDER
  RECOVERY); Gate 3 still PAUSED; `AVATAR_R2` `false`.**
  (3) ⏸️ **PAUSED — the remaining face / eyes / eyelid / hair layers.** Gate-3 work is halted pending the
  recovered base. The hair/eyes/face **tooling** (`build-hair-clean.mjs`, `build-eyes-clean.mjs`,
  `build-face-clean.mjs`) remains useful, but its **outputs are NOT approved layers** against the new
  basis (they were Master-derived + iter7-composited → must be re-derived/validated after registration).
  (4) ✅ a **WebP encoder** — **SATISFIED (2026-07-02):** vendored libwebp `cwebp.exe` 1.5.0
  (`tools/avatar/vendor/`, gitignored; reproducible via `tools/avatar/fetch-cwebp.mjs`) + wrapper
  `tools/avatar/encode-webp.mjs`. Proven: Phase-1 base 242 KB PNG → 37.7 KB WebP (512×768, alpha
  preserved, within the <350 KB budget). Zero npm deps. Build tooling only — no `assets/avatar-r2/`
  write, no `R2_MANIFEST` change, `AVATAR_R2` untouched.
  (5) ⛔ human **visual sign-off** on the composed raster stack — open.
  **Gate 2 is REOPENED (iter7 invalidated) — NOT satisfied.** Phase-2 runtime code may not start. The
  immediate blocker is the **base recovery** (D-043): re-register + neutralize + re-review the adopted
  `recovery-base-v1-blankface.png`; then the remaining art layers (gate 3) and the composed visual
  sign-off (gate 5). Throughout: **Phase-2 implementation is still NOT started**; no Phase-2 runtime
  code; **`AVATAR_R2` stays `false`**; the Phase-1 PNG baked base + pilot opt-in + C2/SVG fallback remain
  intact.
- **Art-production handoff written (2026-07-02, art policy revised D-042) — for gates 2–3.**
  [`167a-phase2-artist-handoff.md`](./167a-phase2-artist-handoff.md) is the practical art-production brief for
  the decomposed layers (v2 base + face/eyes/eyelid/hair): filenames/dims/transparent-bg, what stays
  identical to Master vs what is removed from the v2 base, the approved eye-box (§8), the 164B.3
  acceptance gate, and producer/delivery/rejection checklists. **Policy (D-042): AI-assisted masked
  decomposition allowed; AI regeneration/redesign forbidden.** Doc only — offline art deliverable; no
  runtime/manifest/flag change; Phase-2 not started.

## Open Questions
- OQ-1: ~~Hybrid vs Full-raster~~ **RESOLVED** — Hybrid Raster + WebP (163A/163D).
- OQ-2: ~~Base redesign vs re-asset~~ **RESOLVED** — raster re-asset from North Star v1.0.
- OQ-3: Onboarding does not expose C2 vocabulary (hair_color picker, C2 hairstyles).
- OQ-4: No cohort / % activation mechanism.
- OQ-5: ~~Neutral, symmetric base POSE must be derived from North Star~~ **RESOLVED**
  (2026-06-15) — Master v1.0 is a near-symmetric front pose; D-029 authorises the
  neutral body-underlayer as a derived production asset (cut in 164B). (164A)
- OQ-6: ~~"Sad / negative" expression vs 151A "never-negative"~~ **RESOLVED** — D-024
  locks a permanent never-negative policy; `sad`/`angry` excluded from MVP (ADR-163F).
- OQ-7: ~~Confirm MVP eye scoping (2 files, face-driven emotion)~~ **RESOLVED**
  (2026-06-15) — D-021 stays locked: MVP eyes = 2 files (`iris.webp` + `fixed.webp`),
  MVP emotion is face-driven. Per-expression eye-shape variants are a **future additive
  capability**, not part of MVP (does not change D-012).

## Current Risks
- R-1 (Medium): Art-direction drift — flat SVG base ≠ North Star (eyes/finish). Being
  resolved by the Hybrid Raster re-asset.
- R-2 (Medium): Onboarding mismatch — users cannot pick C2 vocabulary.
- R-3 (Low): Visual/cosmetics regression — mitigated by goldens + flag OFF.
- R-4 (Low-op): Migration-history drift (ledger) — `db push` blocked; MCP apply only.
- R-5 (Low-op): Two-clone sync discipline (ff-pull after each push).
- R-6 (High, art): AI style drift + decomposition seams across raster assets — mitigate
  by producing all layers from one North Star + style-lock + gatekeeper + golden QA.
  **CONFIRMED for the base (164C):** **unmasked** AI generation/regeneration reliably drifts the
  base's proportions/identity. **Mitigation revised (D-042, 2026-07-02):** since no human painter is
  available, the base + rig are produced by **AI-assisted MASKED decomposition on the frozen Master**
  (masked inpainting/outpainting that lifts features + reconstructs what they hid, identity-locked to
  the Master) — **not** free regeneration, which stays forbidden. Residual drift risk is caught by the
  **164B.3 gate** + composed visual sign-off (gate 5). (Old rule: manual paint-over only, D-033.)
- R-7 (Medium, tech): Hair/iris tint quality (canvas multiply + fixed highlight) —
  mitigate via hybrid tint + hand-painted overrides; prototype early.
- R-8 (Medium, art/process): Style coherence across a 1000+ AI-generated item catalog
  (R-6 applied to Tier 2) — mitigate via the locked style kit + per-slot masks + the
  slot-mask/QA gates + style-conformance scoring (D-034 / `docs/164d-shop-pipeline.md`).
- R-9 (Medium, product): MVP identity deferral (D-040) — Tier-0 = Master as a **single
  fixed avatar** + accessories; per-user skin tone / hairstyle / hair-color variation is
  **deferred** with the neutral-base upgrade. Accepted for the pilot MVP; revisit if
  per-user identity at launch becomes a hard requirement.

## Technical Debt
- TD-1: Legacy cosmetic assets are pseudo-3D (clash with flat C2); flat redesign
  pending (armor/cape fit "Needs Adjustment").
- TD-2: Expression assets are legacy-styled (aligned, not flat).
- TD-3: Migration-history drift; repo migration files ≠ DB version ledger.
- TD-4: Gitlink anomaly in ROOT clone (vestigial, not on deploy path).
- TD-5: Local prod-data snapshot `backups/…` kept out of repo (no `.gitignore` yet).
- TD-6: Flat-SVG C2 asset set is **superseded** by the Hybrid Raster pipeline (kept
  for rollback during transition; remove once raster ships).

## Next Recommended Section
**164L — Anchor + MVP mask extraction tooling** (the method is locked by **D-041 / 164K**).
A small **deterministic, non-AI** script reads `Northstar Master.png` (verified 1024×1536) and
emits `avatar-anchor-template-v1.json` + an anchor-overlay preview + QA/build mask PNGs for the
5 MVP accessory slots (`aura`, `back`, `headwear`, `face/masks`, `eyes/glasses`) per the D-037
rules, for human review against the mask worksheet. Then proceed to the Tier-2 AI item-overlay
conveyor. **No illustrator, no manual per-item editing; no AI geometry; `AVATAR_V2` stays OFF.**
This is the first code step and requires explicit approval (no tooling written under 164K).

> **Deferred upgrade (was the old "Next Section", now NOT an MVP blocker — D-040):**
> **164B — Cut & Export the Neutral Stack** — decompose Master into `body-neutral-medium`
> (D-029), `face-neutral`, `eyes-neutral-iris`/`-fixed`, `eyelid-medium`, `hair-northstar-v1`
> at the locked z-stack (D-030), produced by **AI-assisted masked decomposition (D-042)** — masked
> edits on the frozen Master, not regeneration — and passing the 164B.3 gate. This
> is the **future quality upgrade** that enables a neutral default outfit + per-user skin
> tone / hairstyle. Geometry source = `Northstar Master.png` ONLY (D-032); the outfit
> reference never drives geometry. Pursue **after** the MVP accessory shop ships.
